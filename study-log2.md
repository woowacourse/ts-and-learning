# 학습 로그

## 학습 로그 #2 - 26.03.25 (수)

**시간**: 03/25 11:00 ~ 11:30, 13:00 ~ 15:00 (약 150분)

**학습 범위**: typescript-exercises #4 ~ #8

### 1. 두번째 학습 전략 설계

#### 1. 개념 이해 (Problem-Driven)

- 코드의 문제점을 파악하고, 어떤 한계를 해결하기 위해 이 개념이 필요한지 목적을 명확히 한다. (예: "외부에서 들어온 알 수 없는 타입을 어떻게 안전하게 사용할까?" -> 타입 좁히기 필요성 인식)

#### 2. 실험 (Output First & i+1)

- 가설을 먼저 세우고 검증한다. 정해진 문제를 넘어 "타입을 지우면 어떻게 될까?" 같은 한 단계 높은 궁금증(i+1)을 실험한다.

#### 3. 에러 유도 및 팩트 체크 (Feedback Loop & Resourceful)

- 정답을 맞혀도 의도적으로 타입을 망가뜨려 에러를 유도한다.
  - Action: TS가 뱉는 에러 메시지 원문을 캡처한다.
  - Fact-check: 내 직관으로 결론 내리지 않고, 반드시 에러 메시지를 구글, 공식 문서, AI에 검색하여 내 가설이 100% 맞는지 공식 용어로 팩트 체크한다.

#### 4. 구조적 회상 (Structural Thinking)

- 가상의 동료에게 "이 방식은 왜 안 되고, 저 방식은 왜 되는지"를 런타임과 컴파일 타임의 차이점 등 구조적 관점에서 말하듯 설명해 본다. (예: "instanceof는 런타임 값 연산자인데, 인터페이스는 컴파일 후 사라지니까 쓸 수 없어")

#### 5. 응용 및 엣지 케이스 탐색 (i+1)

- 학습한 개념의 극한을 테스트한다. (예: "공통 프로퍼티로 좁히기를 시도하면 도달할 수 없는 곳은 어떻게 될까?" -> `never` 타입 스스로 발견)
- 내가 배운 원리가 작동하지 않는 예외 상황을 상상해 본다.

#### 6. 휴식 및 다음 질문 도출 (Feedback Loop)

- 학습 내용을 회상하며, "그렇다면 다음에는 어떤 문제가 발생할까?"라는 질문을 남기고 휴식한다.

### 2. 2일차 학습의 목표

- [x] 보너스 문제 도전해보기
- [x] 학습법을 지키면서 메모를 적극적으로 작성해보기

### 문제 풀이 메모

#### [ts-exercises-04]

#### 메모1

- 에러 메세지로 Person 타입에 role도 없고 `occupation`도 없다고 한다. logPerson 메서드 안에서 타입 좁히기를 사용해야 하지 않을까?
- 그렇다면 isAdmin과 같은 함수에서 boolean을 반환하는 것이 의미가 없는 걸까? 참/거짓만으로 타입을 판단할 수 없는 것 같다.
- 타입 가드를 사용하면 좁힐 수 있지 않을까? -> boolean이 아니라 반환값이 true이면 특정 타입임을 유추할 수 있도록 `is`를 사용하자.
- 문제가 해결됐다. 그럼 is 말고 다른 방법은 없을까? as를 사용할 수도 있을 것 같다. (근데 as는 강제로 타입을 알려주는 것과 비슷해서 올바른 방법은 아닌 것 같다)

#### [ts-exercises-05]

#### 메모1

- {age:23}은 User 타입으로 정의될 수 없기 때문에 에러가 발생한 것 같다. -> User의 일부 속성만 받아야 문제가 해결될 것 같다.
- 일부 속성을 받는 방법이면 Partial을 사용해보면 될 것 같다. -> Partial 사용법을 까먹어서 `Partial.`, `Partial()`이런 식으로 작성해봤다. 근데 타입스크립트에서 타입을 사용할 때 '<>'를 사용했던 게 기억이 나서 작성했더니 문제가 해결되었다.

#### 메모2

- 보너스 문제도 도전해보자. 타입을 사용하지 말라고 한다. User 타입에서 `type` 프로퍼티를 제거하려면? Partial이 아니라 Omit을 사용하면 될까?
- `type` 프로퍼티를 제거해도 여전히 선택적으로 속성을 받아올 수 있어야 한다. -> `Partial<Omit<User, 'type'>>`으로 정의하면 되겠구나!
- 그렇게 수정하니 이젠 밑에서 에러가 발생한다. `criteria`가 `type` 속성을 가지고 있지 않은데 `const criteriaKeys = Object.keys(criteria) as (keyof User)[];`에서 `keyof User`가 문제가 되는 것 같다. 이 부분을 `keyof Omit<User, 'type'>`로 `type`을 제거하니 에러가 사라졌다.

#### [ts-exercises-06]

#### 메모1

- 06번 문제도 타입의 일부만 가져올 수 있어야 할 것 같다. 인자로 받는 personType 변수가 unknown 타입인데, 실제로는 'user'나 'admin'만 받을 수 있기 때문에 우선 personType을 좁혀야 할 것 같다. -> 두 문자열만 들어 올 수 있다면, `'user' | 'admin'`라고 작성하면 될 것 같다.
- 그렇지만 두 경우에 따라 criteria을 정의할 수 있는 타입이 서로 달라야 할 것 같은데, 어떻게 풀어야 하는지 감이 오지 않는다. GPTs에게 물어보니 오버로드를 사용하면 해결이 가능하다고 한다. 내가 아는 그 메서드 오버로딩이면 해결할 수 있을 것 같다.
- 문제의 개요를 파악하기 힘들었지만, 주석 설명으로는 personType에 따라서 메서드의 반환값이 달라져야 하니까 내 생각에 더욱 확신을 가질 수 있었다.

#### 메모2

- 그럼 보너스 문제에 대해서는? `더 이상 타입 단언(cast)을 하지 않아도 되게 하세요.` 라고 요구사항이 적용되어 있다. 인자로 전달한 객체의 키의 타입을 반환해주는 역할로 바꿔야 할 것 같다.
- 객체의 타입을 반환하는 문법은 `T[]`, 그렇다면 키값은? `(keyof T)[]` 일 것 같다. 근데 그 뒤를 전혀 모르겠다.
- AI를 사용하지 않고는 풀지 못할 것 같아서 일단 건너뛰기로 한다.

#### [ts-exercises-07]

#### 메모1

- 동일한 타입이 아닌 객체들에 대해서 swap이 작동하려면 어떤 것이 필요할까? -> 처음 생각난 건 제네릭인 것 같다.
- 그치만 두 종류의 타입에 대해 작동해야 하므로 제네릭이 2개가 필요하지 않을까? 일단 작성해보자.
- `swap<T, V>(v1: T, v2: V): (V | T)[]`라고 작성해봤다.
- `'User | Admin' 형식의 인수는 'User' 형식의 매개 변수에 할당될 수 없습니다.` 라고 에러가 난다.
- 반환됐을 때의 타입이 정해져야 에러가 없어질 것 같다. 반환값을 특정하는 방법? `[V, T]`로 하면 특정될 것 같은데? 오 작동했다 나이스!
- 제네릭을 아예 몰랐다면 AI를 사용했을 것 같은데, 제네릭에 대해 대충 알고 있어서 풀었던 것 같다.

#### [ts-exercises-08]

#### 메모1

- 새로운 타입이 추가됐다. User와 Admin의 모든 필드를 가져야 하지만 `type` 프로퍼티는 없어야 한다. 모든 필드를 다시 쓰지 말고 `type: 'powerUser'`를 가져야 한다고 한다. 처음 생각나는건 `Omit -> Partial` 순서일 것 같다. 공식 문서를 읽어보자.
- 유틸리티 타입과 인터섹션에 관한 문서이다. `type` 프로퍼티를 제외한 모든 필드를 가져야 한다고 했으니, &(인터섹션)으로 합친 뒤 `Omit`으로 `type`을 제거하면 되지 않을까? 시도해보자.
- PowerUser 타입의 모든 프로퍼티가 never가 되었다. 왜지..? -> 이번에는 각각에서 타입을 제거한 뒤 합쳐보자. 근데 이러니 PowerUser 안에 type 프로퍼티가 없다.
- 그럼 똑같이 인터섹션으로 추가하면 되지 않을까? 오 해결됐다!

#### 메모2

- 이런 과정이 되게 비효율적인 것 같은데, 다른 방법이 없을까? -> AI에게 물어보자
- 이 문법이 가장 간결하다고 한다. 추가로 공통 프로퍼티가 존재하는 채로 인터섹션을 하면 그 프로퍼티 타입이 never가 된다는 사실도 실험을 통해 깨달았다.

### 3. 전략 평가

- 효과적이었던 것과 그 이유

  - 에러 던지기 및 메모 작성: 내가 어떤 생각을 가지고 그런 결정을 내렸는지 확인해볼 수 있음

- 비효과적이었던 것과 그 이유
  - 회상 단계와 엣지케이스 단계: 실제로 잘 이루어지지 않음

### 4. AI 피드백

#### 1. [Feedback Loop & Structural Thinking] 배열과 '튜플(Tuple)'의 차이 스스로 깨닫기 (#7)

- 상황: swap 함수에서 반환 타입을 (V | T)[]로 했다가 에러를 맞고, [V, T]로 수정하여 해결.

- 코치의 시선: 이번 로그의 최고의 명장면입니다! (V | T)[]는 "V나 T가 섞여 있는 길이 제한 없는 배열"을 의미하고, [V, T]는 "정확히 첫 번째는 V, 두 번째는 T인 길이가 2인 튜플"을 의미합니다. 컴파일러가 왜 전자를 거부했는지(순서와 길이가 보장되지 않으므로) 에러를 통해 구조적으로 완벽히 깨달으셨습니다.

#### 2. [Output First & i+1] 교집합(Intersection)과 never의 관계 증명 (#8)

- 상황: User와 Admin을 그대로 &(교집합) 했더니 type 프로퍼티가 never가 된 것을 발견.

- 코치의 시선: TS의 타입 시스템이 집합론(Set Theory)에 기반한다는 것을 실험으로 증명해 내셨습니다! type: 'user'이면서 동시에 type: 'admin'일 수는 없으니, TS가 "이건 불가능한 타입이야!"라며 never를 뱉어낸 것입니다. 이 원리를 직접 몸으로 부딪쳐 알아낸 것은 엄청난 수확입니다.

#### 3. [Resourceful] 막힌 곳에서 쿨하게 끊어가기 (#6 보너스)

- 상황: 객체 키의 타입을 반환하는 문법에서 막힌 뒤 일단 건너뜀.

- 코치의 시선: 아주 좋은 태도입니다. 한 문제에 매몰되어 전체 스프린트의 흐름(Flow)을 깨지 않고, 자신의 현재 한계를 인지한 메타인지가 훌륭합니다.

### 5. 다음 타임에 바꿀 것

- 유지할 것과 그 이유

  - 실험 단계와 에러 유도 및 팩트 체크 단계: 이것저것 시도해보고 실패하고 피드백 받는 구조가 사이클이 잘 돌아가는 것 같기 때문

- 바꿀 것과 그 이유

  - 회상 단계와 엣지케이스 단계를 과감히 제거:
    - 새로운 개념을 배운 경우에는 응용이 쉽게 떠오르지 않기 때문
    - 설명하는 것도 단순히 방금 배운 내용을 설명하는 것은 의미 없다고 생각했기 때문이다.

- 추가할 것
  - [주석 -> 코드 읽기 -> 마지막 문서 읽기] 후 문제 풀이 시도
  - 진짜 모르면 GPTs에 키워드 하나 넣고 과정 유도시키기

---

### 다음부터 적용할 개선된 학습법

#### 1단계: 의도 파악 (Problem-Driven)

- 행동: [주석 읽기] -> [코드 흐름 파악] -> [필요한 공식 문서 훑기]
- 핵심 질문: "출제자가 지금 나한테 무슨 타입스크립트 기술을 요구하는 걸까?"

#### 2단계: 충돌 테스트 (Output First & Feedback Loop)

- 행동: 완벽한 답을 찾으려 하지 말고, 일단 생각나는 대로 코드를 던져보고 에러를 맞아보기
  - 여기서 발생한 에러 메시지만 학습 로그에 툭툭 던져놓듯 메모하기

#### 3단계: 키워드 SOS & 팩트 체크 (Resourceful & Structural Thinking)

- 행동 1 (막혔을 때): 제안하신 대로 GPT에 핵심 키워드나 에러 메시지만 하나 툭 던지고 "정답 말고 다음 단계로 갈 힌트만 줘"라고 유도한다. 이 때 메모로 [에러 - 힌트 - 실험] 과정을 기록한다.
- 행동 2 (풀었을 때): 내 직관으로 푼 게 맞는지 문서나 GPT에 물어보고, 한두줄 정도의 깨달음만 메모하고 넘어간다.

#### 4단계: 1분 마이크로 회상 & 상황 인덱싱 (Active Recall & i+1)

- 행동 1: AI 프론트엔드 마이크로 퀴즈

  - 억지로 응용 사례를 쥐어짜지 말고, GPT에게 아주 짧은 퀴즈를 요구해서 빠르게 풀고 넘어간다.
  - GPT 프롬프트 템플릿:
    > "내가 방금 TS에서 [ 오늘 배운 개념, 예: Omit ]을 배웠어. 이게 실제 개발에서 어떻게 쓰이는지 확인하고 싶어. 정답을 주지 말고, 고의로 타입 에러가 나는 3~4줄짜리 아주 짧은 코드를 퀴즈로 내줘. 내가 직접 수정해 볼게."

- 행동 2: 나를 위한 한 줄 요약
  - 퀴즈를 풀고 난 직후, "아, 이럴 때 쓰는 거구나!"라는 깨달음을 '상황 + 도구' 포맷의 딱 한 줄을 학습 로그에 작성한다.
  - 작성 예시:
    > \* "남이 만든 복잡한 타입에서 딱 한 개 프로퍼티만 빼고 싶을 때 ➡️ Omit 꺼내기"
    > "런타임에 들어오는 값이 A인지 B인지 몰라서 빨간줄이 날 때 ➡️ is 타입 가드 쓰기"
