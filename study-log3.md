# 학습 로그

## 학습 로그 #2 - 26.03.26 (목)

**시간**: 03/26 09:40 ~ 10:00, 13:00 ~ 14:30 (약 130분)

**학습 범위**: typescript-exercises #9 ~ #12

### 1. 세 번째 학습 전략 설계

#### 1단계: 의도 파악 (Problem-Driven)

- 행동: [주석 읽기] -> [코드 흐름 파악] -> [필요한 공식 문서 훑기]
- 핵심 질문: "출제자가 지금 나한테 무슨 타입스크립트 기술을 요구하는 걸까?"

#### 2단계: 충돌 테스트 (Output First & Feedback Loop)

- 행동: 완벽한 답을 찾으려 하지 말고, 일단 생각나는 대로 코드를 던져보고 에러를 맞아보기
  - 여기서 발생한 에러 메시지만 학습 로그에 툭툭 던져놓듯 메모하기

#### 3단계: 키워드 SOS & 팩트 체크 (Resourceful & Structural Thinking)

- 행동 1 (막혔을 때): 제안하신 대로 GPT에 핵심 키워드나 에러 메시지만 하나 툭 던지고 "정답 말고 다음 단계로 갈 힌트만 줘"라고 유도한다. 이 때 메모로 [에러 - 힌트 - 실험] 과정을 기록한다.
- 행동 2 (풀었을 때): 내 직관으로 푼 게 맞는지 문서나 GPT에 물어보고, 한두줄 정도의 깨달음만 메모하고 넘어간다.

#### 4단계: 1분 마이크로 회상 & 상황 인덱싱 (Active Recall & i+1)

- 행동 1: AI 프론트엔드 마이크로 퀴즈

  - 억지로 응용 사례를 쥐어짜지 말고, GPT에게 아주 짧은 퀴즈를 요구해서 빠르게 풀고 넘어간다.
  - GPT 프롬프트 템플릿:
    > "내가 방금 TS에서 [ 오늘 배운 개념, 예: Omit ]을 배웠어. 이게 실제 개발에서 어떻게 쓰이는지 확인하고 싶어. 정답을 주지 말고, 고의로 타입 에러가 나는 3~4줄짜리 아주 짧은 코드를 퀴즈로 내줘. 내가 직접 수정해 볼게."

- 행동 2: 나를 위한 한 줄 요약
  - 퀴즈를 풀고 난 직후, "아, 이럴 때 쓰는 거구나!"라는 깨달음을 '상황 + 도구' 포맷의 딱 한 줄을 학습 로그에 작성한다.
  - 작성 예시:
    > \* "남이 만든 복잡한 타입에서 딱 한 개 프로퍼티만 빼고 싶을 때 ➡️ Omit 꺼내기"
    > "런타임에 들어오는 값이 A인지 B인지 몰라서 빨간줄이 날 때 ➡️ is 타입 가드 쓰기"

### 2. 2일차 학습의 목표

- [ ] 문제에 대해 전혀 모르면 전부 물어보지 말고 키워드만 AI에게 던지기
- [ ] 풀이하면서 든 생각 및 과정을 메모하기
- [ ] 문제별 공식 문서 정독하기

### 문제 풀이 메모

#### [ts-exercises-09]

#### 메모1

- `AdminsApiResponse` 타입과 `UsersApiResponse` 타입을 지우고 `ApiResponse` 타입을 완성하라고 한다. 두 타입에서 다른 부분은 `success` 시의 data의 타입이다.
- 그럼 제네릭으로 { status: 'success'; data: T[]; }와 { status: 'error'; error: string; }를 유니온하면 되지 않을까?
- 그리고 메서드에서 인자의 타입을 ApiResponse<>으로 정의해준 뒤 각각의 타입을 넣어봤다. 여전히 같은 에러가 발생한다.
- 일단 data 부분을 유심히 봤는데, requestCurrentServerTime와 같은 함수는 배열을 data로 넣지 않는다. `T[]`가 아닌 `T`라고 해야 할 것 같다.
- 나머지 `requestCurrentServerTime`, `requestCoffeeMachineQueueLength`에도 적용해보자. 각각 타입을 number, number라고 하니 문제가 안생긴다. 이상하네?

#### 메모2

- `requestCoffeeMachineQueueLength` 메서드의 타입을 boolean으로 바꿔봤다. 문제가 생기지 않는다. 생각해보니 이 메서드에서는 success인 경우를 반환하지 않는다. 그래서 T가 무시되는 게 아닐까?
- 만약 success도 반환한다고 가정하면, 타입은 number가 되어야 한다. (메서드 이름부터가 길이를 요청하는 것이니까)

#### [ts-exercises-10]

#### 메모1

- 일단 보너스 문제 말고 일반 문제에 도전해보자. 기존의 콜백 기반 함수들을 감싸서 새로운 Promise 호환 결과를 반환하게 하라고 한다.
- `promisify` 메서드의 인자 타입과 반환 타입을 수정해야 될 것 같다. 근데.. 너무 어렵다. 어디서부터 시작해야 될 지 몰라서 GPT한테 우선 '메서드를 어떻게 수정해야 하는지 개요를 알려줘'라고 부탁했다.
- 인자인 `arg`의 타입을 유추하는 것은 가능했다. 아래 기존의 메서드 타입을 보고 유추하는 것은 어렵지 않았다. 근데 함수의 내용을 구현할 때 또 막혔다.
- 또 GPT를 불렀다. 힌트를 요청하니 new Promise<T> 객체를 반환하는 메서드를 return하라고 한다. 그럼 resolve와 reject 두 가지 경우로 나눠야 하기 때문에 if문으로 분기시켰다.
- 그럼 resolve와 reject의 인자로는 어떤 걸 전달해야 하지? 그래서 각각 response.data와 response.error를 넣어봤다.
- 통과하네? 실제 ts-exercises 웹에 들어가서 적용해보니 통과했다.

#### 메모2

- 그래서 reject() 메서드를 아예 주석처리하고 넣어봤는데도 작동했다. 왜 그런지는 모르겠다. 10번 문제에 대해서는 미션을 완료한 후 복습하고, 그 이유까지 찾아봐야겠다.
- 문제에서 타입 부분을 다 지우고 원형만 보았더니 그제서야 내가 알던 함수의 모습이 보였다. 타입 부분을 다 지우고 원형만 먼저 보고 이해하는 방식도 좋은 방법인 것 같다.

#### [ts-exercises-11]

#### 메모1

- `str-utils` 모듈에서 5개의 메서드를 모두 구현하면 되는 문제이다. 대신, 타입스크립트이기 때문에 타입까지 정의해야 한다. 문제 조건에서 README.md를 읽고 index.d.ts에서 구현하라고 한다.
- 5개의 메서드가 모두 문자열을 value로 받아 변환된 문자열을 반환하기 때문에 두 타입 모두 string이면 될 것 같은데?
- 모듈 내부에 `StringDecorator` 타입을 선언해서 `(value: string) => string`라고 작성했다. 그리고 각각 구현하려고 했는데, 내용 구현하는 게 문제의 목표가 아니었기 때문에 선언하는 식으로 변경해야 할 것 같다.

#### [ts-exercises-12]

#### 메모1

- 11번 문제와 비슷하게 명세를 보고 메서드의 타입을 추론하면 될 것 같다. 차이점이 있다면 모든 메서드의 인자, 반환 타입이 동일하지 않다는 것이다. element, index, value등의 변수가 각자 다른 타입을 가지도록 선언해야 한다.
- 일단 보너스 문제를 고려하지 않고 문제를 푼다면, 단순히 3개의 타입을 선언하면 될 일이다. 선언한 뒤 반환값을 `Union`으로 처리하면 되지 않을까? 적용해보자.
- 근데 단순해보였는데 User, Admin 타입 두 가지를 공통 로직으로 처리해야 하니까 제네릭을 사용했다. 그리고 메서드가 너무 길어지니까 `Comparator` 타입을 따로 분리했다.
- 분리하고 보니 맨 마지막 메서드인 `getAverageValue` 메서드는 다른 메서드와 다르게 getValue를 인자로 받고 있다. `index.ts`를 보니까 comparator와 다르게 생겼다. 그래서 `Selector` 타입을 추가로 정의했다.
- 이후 메서드의 성격에 맞게 3 3 1로 나눠서 반환값을 다르게 설정했다. 근데 7개의 메서드를 모두 작성하는 것이 비효율적이라고 생각이 들었다.

#### 메모2

- 중복을 최소화할 수 있는 방법에 대해 생각해봤다, 제네릭 인자를 2개를 사용하는 방법? 도 생각해봤는데 그럼 코드가 안읽히지 않나? 생각이 들었다. 내가 생각하기엔 `(input: T[], comparator: Comparator<T>): number`이 부분을 타입으로 정의해서 가독성을 높이는 방법밖에 없는 것 같다. 이 문제도 따로 메모를 해놓아야겠다.

### 3. 전략 평가

- 효과적이었던 것과 그 이유

  - 학습법 단계가 늘어지지 않아서 실행하기 더 편해짐
  - 막혔을 때 AI 도구가 키워드나 역질문을 던질 수 있도록 의도한 것

- 비효과적이었던 것과 그 이유
  - 어떤 새로운 사실을 알았을 때 검증 단계를 제대로 실행하지 못한 것
  - 왜 통과했는지를 모르는 코드에 대해 메모만 하고 넘어가는 것..?

### 4. AI 피드백

Feedback Loop & Structural Thinking: "통과하네? 왜 그런지는 모르겠다"의 함정 주의

"reject() 메서드를 아예 주석처리하고 넣어봤는데도 작동했다. 왜 그런지는 모르겠다. (...) 완료한 후 복습하고, 그 이유까지 찾아봐야겠다." (ts-exercises-10)

이 지점이 바로 다음 학습에서 가장 먼저 교정해야 할 학습 병목입니다. 코드가 우연히 통과했을 때 "왜 되지?"라는 의문을 뒤로 미루면, 피드백 루프가 끊어집니다. 특히 타입스크립트에서는 이 의문이 **'컴파일 타임과 런타임의 차이'**라는 핵심 개념과 맞닿아 있습니다.

### 5. 다음 타임에 바꿀 것

- 유지할 것과 그 이유

  - GPTs ts 학습 도구는 유지하기
  - 1~3단계는 유지

- 바꿀 것과 그 이유

  - 4단계의 행동2가 제대로 수행되기 어려운 것 같음 <- 다른 방식 생각해보기

- 추가할 것

  - Gems보다 GPTs로 학습법 개선 에이전트 만들어보기

---

### 다음부터 적용할 개선된 학습법

#### 1단계: 의도 파악 (Problem-Driven)

- 행동: [주석 읽기] ➡️ [코드 흐름 파악] ➡️ [필요한 공식 문서 훑기]
- 핵심 질문: "출제자가 지금 나한테 무슨 타입스크립트 기술을 요구하는 걸까?"

#### 2단계: 충돌 테스트 및 '우연한 성공' 검증 (Output First & Feedback Loop) 🌟[업그레이드]

- 행동 1: 완벽한 답을 찾으려 하지 말고, 일단 생각나는 대로 코드를 던져보고 에러를 맞아보기. (발생한 에러 메시지 툭툭 메모)
- 행동 2 (검증): 만약 코드가 에러 없이 통과했는데 '왜 통과했는지' 명확히 설명할 수 없다면, 즉시 진도를 멈춘다. GPT에게 "이게 왜 통과하는 거야? TS 컴파일 타임과 관련이 있어?"라고 물어보고 찝찝함을 해소한 뒤 넘어간다.

#### 3단계: 키워드 SOS & 팩트 체크 (Resourceful & Structural Thinking)

- 행동 1 (막혔을 때): GPT에 핵심 키워드나 에러 메시지만 하나 툭 던지고 "정답 말고 다음 단계로 갈 힌트나 역질문을 던져줘"라고 유도한다. [에러 - 힌트 - 실험] 과정 기록.
- 행동 2 (풀었을 때): 내 직관으로 푼 게 맞는지 문서나 GPT에 교차 검증한다.

#### 4단계: 마이크로 퀴즈 & Before/After 스냅샷 (Active Recall & i+1) 🌟[리팩토링]

- 행동 1: AI 프론트엔드 마이크로 퀴즈

  - GPT에게 "방금 배운 [개념]에 대해 고의로 타입 에러가 나는 3~4줄짜리 짧은 퀴즈를 내줘"라고 요구하고 빠르게 푼다.

- 행동 2: Before & After 코드 스냅샷 남기기 (글쓰기 ❌, 코드 복붙 ⭕)

  - 억지로 문장으로 요약하지 말고, **내가 틀렸던 코드(또는 몰랐던 상태)**와 해결한 코드를 나란히 배치하고, 딱 한 줄의 주석만 단다.ㄴ
  - 작성 예시 (학습 로그에 이렇게 남기기):

    ```
    // ❌ Before (에러 났던 방식/오해했던 부분)
    type ApiResponse = { status: string; data: T[] }
    // 💡 깨달음: 아, 항상 배열(T[])로 오는 게 아니라 단일 객체(T)로 올 수도 있구나!

    // ✅ After (해결책)
    type ApiResponse<T> = { status: 'success'; data: T } | { status: 'error'; error: string }

    ```
