# 학습 로그

## 메타인지 하기

### 1. 현재 TypeScript 경험

- TS를 써본 적이 있는가? 있다면 어느 정도?
- 난생 처음입니다.

### 2. 기존에 새로운 개념을 학습할 때 어떻게 했나요? (수업시간에 v1 떠올렸던 것 첨부)

- 기존에 새로운 개념을 학습할 때에는 인강과 책을 통해 학습하고 기록하면서 학습했습니다.

## 학습 로그 #1

**시간**: 03/25 10:30 ~ 13:40 (점심시간 제외 약 130분)
**학습 범위**: typescript-exercises #1~#3

### 1. 첫 학습 전략 설계
- 어떤 전략으로 학습하기로 했는가?
  - 타입스크립트를 처음 마주하는 입장으로서, 기초적인 개념을 "러닝 타입스크립트" 책을 읽어가며 개념학습
  - 코드 하단에 주어진 공식문서 노트북LM으로 정리해서 원하는 개념 뽑아먹기
  - 코드를 정독하며 무엇을 의도한건지 생각해보기
- 그 전략을 선택한 이유는?
  - 기존에 진행하던 개념학습방법을 기반으로 정확한 공식문서를 참고하면서 학습하기 위해
- 실제로 어떻게 학습했는지 디테일한 과정을 써보세요.

#### 1번 문제
##### AI gems 질문과 답변 키워드
1. 타입스크립트에서 자료형을 지정하는 문법은 뭐야? 
- 타입 어노테이션
- 기본 타입
- 타입 추론
2. 한번에 여러개의 타입을 지정해두고 사용하는 문법이 어떤지 실제 코드예를 통해 보여줘!
- 유니언 타입
- 타입 별칭
- 파이프 기호
- type 키워드
3. 모범답안으로 type User = {...} 이 아니라 interface User {...} 과같은 형태로 타입을 선언하던데, 이건 뭐야?
- 인터페이스
  - 객체의 구조를 정의하는 또다른 대표적인 방법
  - 확장성 : 기존에 만들어둔 타입에 새로운 속성을 쉽게 추가할 수 있는 능력이 있다.
##### 학습한 것
- 타입을 어떻게 지정하는지 gems튜터와 함께 학습했다. 하나의 변수에 파이프( | ) 를 이용하여 여러가지 타입을 할당해놓을 수 있음을 알게 되고, 복잡한 타입을 매번 길게 적지 않고 재사용할 수 있게 해주는 타입 별칭(alias)에 관해서도 알게 되었다.
- 이 때, type을 이용해 타입 별칭을 미리 선언해두고, 추후에 사용할 수 있게 하는 것임을 알게 되고, export type User 는 뒤에서 사용될 예정임을 알게 되었다.
- 초기 선언을 string | number 로 지정했더니 오류가 발생했다. string, number, boolean과 같이 단일값을 나타내는 원시 타입은 객체 타입을 나타내는 데 사용할 수 없었다. User 안에 name, age, occupation과 같은 객체의 속성을 담고 있기 때문에 각 속성별로 원시타입을 지정해주어야 했다. 결과적으로 name, age, occupation 각각 속성을 할당해줌으로써 테스트를 통과할 수 있었다.
- 모범 답안을 확인한 뒤, 내가 선언한 type과 다르게 interface를 사용하는 것을 확인했다. type같은 경우, 유니언이나 원시 타입같은 모든 타입에 별명을 붙일 수 있지만 이름을 붙여서 닫아버리면 추후 속성을 추가하기가 까다롭다. 그에 반해 interface는 객체의 형태를 정의하는 데 특화되어 있고 선언 병합이 가능하다. 추후 필요한 객체의 속성만 선언한다면, 이전에 선언했던 타입에 추가로 붙는 형태로 병합된다.

#### 2번 문제
##### AI gems 질문과 답변 키워드
1. 좋아 이번 문제는 정말 간단하게 해결했는데, 내 완성된 코드를 보여줄게. 여기서 내가 더 배울 수 있는 키워드가 있을까?
- 타입 좁히기 (Type Narrowing)
- in 연산자를 활용한 타입 가드
##### 학습한 것
- 1번 문제에서 학습한 유니언 타입을 이용해 Person에 User와 Admin의 타입을 합쳐 사용했다.
- 현재 person은 User일수도, Admin일수도 있는 넓은 유니언 타입이다. 런타임 동작을 기반으로 이 넓은 타입을 특정한 하나의 타입으로 좁혀서 유추해 내는 과정이 내로잉이다. 
- in 연산자는 객체 안에 특정 속성이 있는지 확인할 수 있다. 타입을 안전하게 좁혀줄 수 있다. 
  - ex) ```if ("role" in person) {...} 구문을 통해 타입스크립트가 role이 있는 타입임을 인지함.```

#### 3번 문제
##### AI gems 질문과 답변 키워드
1. 방금 알려준 in 키워드를 활용해 이번에도 쉽게 해결했어. 내가 추가적으로 더 알아야 할 키워드가 있을까? 놓치고 지나간게 있는지 알려줘.
- 식별 가능한 유니언 (Discriminated Unions)
  - 여러 개의 타입을 묶을 때, 공통된 속성을 넣어서 이름표처럼 사용하는 기법
  - User에 type: "user" 와 같은 속성을 추가해서 추후 if문에 person.type === 'user' 와 같은 조건문을 거는 것
- 사용자 정의 타입 카드 (Type Predicates / is 키워드)
  - in이나 typeof를 쓰는 것을 넘어, 이 함수를 통과하면 무조건 Admin 타입이야! 와 같이 타입스크립트에게 명시적으로 알려주는 나만의 함수를 만드는 문법
  - ex) ```function isAdmin(person: Person): person is Admin { return "role" in person; }```
##### 학습한 것
- 2번 문제에서 미리 추가적으로 알아야 할 키워드 질문을 통해 '내로잉'과 'in'을 배웠고, 이를 활용해 유니언 타입을 내로잉하는 방법을 실습했다.

### 2. 전략 평가
- 효과적이었던 것과 그 이유
  - AI에게 궁금한 것을 물어보고, 그것을 키워드로 뽑아내도록 사전에 설정해두었는데, 이를 통해 문제를 푸는 데 필요한 키워드를 핵심만 뽑아서 빠르게 습득할 수 있었다.
  - 문제를 모두 푼 뒤에, 내가 추가적으로 학습해야 할 것을 키워드로 정리해서 예습해두니, 다음 문제에서 바로 사용하는 등 놓치는 것이 없도록 상세하게 따라가며 습득할 수 있었다.

- 비효과적이었던 것과 그 이유
  - 러닝 타입스크립트 책 초반을 조금 읽고 시작했는데, 실제 코드를 치면서 사용하는 것보다 시간도 오래걸리고 효율이 잘 나오지 않는 느낌이었다. 공식문서를 정리한 노트북LM과 타입스크립트 튜터 gems를 활용하는 것이 너무 압도적으로 편리했다..  
  - 코드의 의도를 분석하려 했으나, 앞서 예습한 개념으로 이미 무엇을 의도하는지 보여버려 너무
  편리한 AI도구가 정말 학습에 도움이 되는것인가 하는 의문이 들었다.

### 3. AI 피드백
- 자신의 학습 전략에 대해 AI에게 피드백을 요청하고, 유용했던 제안 1가지 이상 기록
"문제를 해결한 직후, 만족하지 않고 '내가 더 알아야 할 키워드/놓친 개념'을 선제적으로 질문하는 것"

### 4. 다음 타임에 바꿀 것
- 유지할 것과 그 이유
  - 문제 해결 이후 추가학습 키워드 묻기 : 가지를 뻗어나가듯이 연관된 키워드로 학습하면 더 기억하기도 쉽고 확장이 용이하다.

- 바꿀 것과 그 이유
  - 책은 읽은 부분까지만 읽고 이후는 실제 코드를 보면서 추후 필요한 부분만 읽는 식으로 변경 : 정독하기에는 시간이 너무 오래걸리고 상세한 지식까지 한번에 학습하려니 머리가 과부하가 왔었다. 나중에 세부사항을 익힌 다음, 이펙티브 책을 통해 깊이 탐구할 때 레퍼런스용으로 활용할 예정이다.
---

## 학습 로그 #2

**시간**: 03/25 14:00 ~ 17:25 (회의시간 제외 약 145분)
**학습 범위**: typescript-exercises #4~#6

### 1. 이번 타임의 학습 전략
- 이전에 바꾸기로 한 전략은 무엇이었고, 실행했는가?
  - 책은 필요할 때 뽑아먹는 효율적인 방법으로 변경
  - 문제 풀이 이후 AI에게 추가학습 키워드 묻기
- 실제로 어떻게 학습했는지 디테일한 과정을 써보세요. 
#### 4번 문제
##### AI gems 질문과 답변 키워드
1. 아까 알려준 is 키워드를 활용해 이번에도 쉽게 해결했어. 내가 추가적으로 더 알아야 할 키워드가 있을까? 놓치고 지나간게 있는지 알려줘.
- 제어 흐름 분석
  - 타입스크립트 코드가 실행되는 순서를 따라가며 타입을 좁혀나가는 기능
- 배열의 filter와 타입 가드의 시너지
- never 타입과 망라성 검사(Exhaustiveness Checking)
  - 모든 가능한 유니언 타입을 다 처리했는지 컴파일러가 검사하게 만드는 궁극의 안전장치
##### 학습한 것
- 처음 테스트를 돌려보았을 때, isAdmin이나 isUser 메서드를 통해 type을 인지하지 못하는 문제 존재. << true와 false와 같은 boolean 값으로 반환되었을 때, if 문 내에서 타입 내로잉이 이루어지지 않았기 때문
- 앞서 예습한 is 키워드를 이용해 person is ** 과 같이 명시해줌으로써 내로잉 코드 학습
- 제어 흐름 분석을 적극 활요한다면, if문 2개가 아니라 else if로 이어주는 것이 더 안전하고 성능상 유리하다.
- filter 함수는 배열을 필터링할 뿐, 타입을 바꿔주지는 못하는데, 반환 타입이 person is Admin인 함수로 인해 filter(isAdmin)을 통해 완벽한 Admin[] 타입을 필터링할 수 있다. 사용자 정의 타입 가드를 쓰는 가장 큰 이유이다.
- 나중에 Guest라는 새로운 타입이 Person에 추가되는 등, 추후 확장에 용이하게 하기 위해 마지막 else 문에 다음과 같이 추가할 수 있다.
```
} else {
        // Person에 User, Admin 외에 새로운 타입이 추가되면 
        // 이 곳으로 떨어지게 되며, 'person'은 never 타입이 됩니다.
        // 이때 컴파일 에러를 발생시켜 개발자에게 "업데이트 잊었어!"라고 알려줍니다.
        const _exhaustiveCheck: never = person; 
        return _exhaustiveCheck;
    }
```

#### 5번 문제
##### AI gems 질문과 답변 키워드
1. 우선, filterUsers에서 criteria라는 속성을 따로 선언했는데, 이렇게 한 이유가 뭘까?
또, Object.keys(criteria) as (keyof User)[] 부분에서 keyof가 정확히 무슨 역할이고 as가 뭔지 알려줘.
마지막으로 every 메서드까지 filterUsers 부분에 대해 초보자도 알 수 있게 자세하게 설명해줘.
##### 학습한 것
- criteria?, Object.keys(criteria) as (keyof User)[];
  - criteria를 통해 추후 필터링을 원하는 조건을 입력받아 filterUsers를 유연한 함수로 만들었다.
  - keyof : 타입스크립트의 타입 조작 기능 중 하나.
    - 어떤 타입이 가지고 있는 모든 속성의 이름을 뽑아서 유니언 타입( | )으로 만들어 준다
    - 즉, 5번 문제의 경우 User 타입의 키인 type, name, age, occupation을 뽑아 유니언 타입으로 만들어 준다
  - as : 타입 단언 (Type Assertion)
    - 타입스크립트에게 내 말대로 믿어! 하고 강제하는 문법
    - Object.keys()가 기본적으로 문자열 배열(string[])을 반환하므로 타입스크립트는 배열 안에 age가 들었는지, apple이 들었는지 확신하지 못한다.
    - as를 사용해서 keys()로 걸러내 온 문자열들이 아무 문자열이 아니라, 무조건 User에서 keyof로 가져온 유니언 타입 중의 하나야! 라고 타입스크립트를 안심시켜 주는 역할인 것이다.
- Partial 키워드를 통해 원하는 속성만 골라내는 방법을 학습함
  - Partial<Type> : 전부 선택적으로 만들기 기능
  - Type의 모든 속성 뒤에 ?를 붙여서 있어도 되고 없어도 되는 선택적 속성으로 바꿔준다.
- Omit 키워드를 통해 원하는 속성을 제외하는 방법을 학습함
  - Omit<Type, Keys> : 특정 속성만 제외하는 기능
  - Type에서 내가 원하지 않는 특정 속성(Keys)만 제거해주는 도구다.

#### 6번 문제
##### AI gems 질문과 답변 키워드
1. 함수 오버로드를 어떻게 사용하는지 자세한 방법과, 함수 오버로드를 사용해서 내가 원하는 타입을 필터링하는 방법을 알려줘
- 오버로드 시그니쳐
  - 껍데기 규칙으로, 이 함수는 A를 넣으면 B가 나오고, 이 함수는 C를 넣으면 D가 나와 하고 외부에 선언하는 부분이다. 구현 코드는 없고 타입만 적는다.
- 구현 시그니쳐
  - 실제 알맹이 로직으로, 오버로드 시그니쳐를 통해서 선언한 규칙을 모두 받아낼 수 있도록 넓은 타입(any나 유니언 타입)을 써서 실제 코드를 작성하는 부분이다.
2. 좋아. 함수 오버로드를 통해 기본 예제는 해결했어. 여기서 내가 추가적으로 더 알아야 할 키워드가 있을까? 놓치고 지나간게 있는지 알려줘.
- 제네릭
  - 타입을 함수의 파라미터처럼 변수화해서 사용하는 문법.
  - 꺽쇠괄호 <T>를 주로 사용하며, 어떤 타입이 들어오든 유연하게 대처할 수 있게 해주는 개념
  - 앞서 배운 keyof 타입 연산자와 함께 제네릭을 결합하면 무슨 객체를 주든, 그 객체의 키만 뽑아올게! 라고 선언할 수 있는 강력한 도구
##### 학습한 것
- 함수 오버로드
  - 선언부와 구현부를 분리해 생각하는 것이 핵심이다.
  - 자바스크립트는 함수 이름이 같으면 맨 마지막에 작성한 함수가 이전 함수를 덮어쓰지만, 타입스크립트는 오버로드를 구현해놓았다.
  - 들어오는 파라미터의 개수나 타입이 다르다면, 다른 타입을 반환하게 만들 수 있다.
  - 이를 이용해서, personType과 criteria가 다르게 들어오면, 다른 타입을 반환하게 만들었다.
- 제네릭
  - 꺽쇠괄호를 사용해서 어떤 타입이든 받아올 수 있는 강력한 사용법을 알게 되었다.

### 2. 전략 평가
- 효과적이었던 것과 그 이유
  - 기본학습이 마무리 된 뒤에, 추가적으로 알아야 할 키워드를 물어봄으로 확장 개념까지 익힐 수 있었다. : 관련된 개념을 확장해나갈 수 있어 단계별 학습에 적합했다.
- 비효과적이었던 것과 그 이유
  - 문제세트가 유명한 것인지, 문제풀이 이후 추가개념을 요청할 때 다음 문제에 나올 개념을 모두 알고 있는 것 같았다. 이전 문제에서 배운 개념이 바로 나오니 풀기는 쉬웠지만, 과연 추후 실적용할 수 있는지 확신이 서지 않았다.

### 3. AI 피드백
- 자신의 학습 전략에 대해 AI 학습 전문가에게 피드백을 요청하고, 유용했던 제안 1가지 이상 기록

"모르는 것을 묻는 데 그치지 않고, 코드의 구성 요소(criteria, as, keyof, every 등)가 '왜' 그렇게 작성되었는지 의도를 집요하게 파고드는 것"

### 4. 다음 타임에 바꿀 것
- 유지할 것과 그 이유
  - 핵심 키워드 중심 학습방법은 유지 : 핀포인트로 지식을 익힌다는 점에서 꽤나 효율적이고, 그럼에도 풀리지 않을 때의 호기심과 추가학습 가능성이 매력적임
- 바꿀 것과 그 이유
  - AI가 지나친 힌트를 주지 않게 하고, 존재하는 연습문제가 아닌 새로운 문제 예시를 통해 학습이 가능하도록 프롬프트 제한
---

## 학습 로그 #3

**시간**: 03/26 09:30 ~ 11:00 (한발짝 스터디 제외 약 60분)
**학습 범위**: typescript-exercises #7~#9

### 1. 이번 타임의 학습 전략
- 이전에 바꾸기로 한 전략은 무엇이었고, 실행했는가?
  - AI에게 지나친 힌트를 주지 않고, 예제 설명 시 새로운 예제를 예로 들도록 변경
- 실제로 어떻게 학습했는지 디테일한 과정을 써보세요. 
#### 7번 문제
##### AI gems 질문과 답변 키워드
1. 음, 네가 이미 너무 유명한 문제라 다 알고 있다는 걸 들어서 나의 학습에 조금 더 집중해보려고 해. 앞으로 지나친 힌트는 자제해주고, 내가 알아야 할 지식을 맛보기로 소개해주는 정도로만 제한해줘. 그리고 만약 예제를 설명한다면 말해준 것처럼 백지상태에서 예제를 들고, 유명한 오픈소스 타입스크립트 연습문제 세트를 직접적으로 들고오지는 말아줘.
- 맛보기 개념 소개
- 독립적인 창작 예제
- 여러 개의 제네릭
- 튜플
2. 완성한 코드를 바탕으로, 내가 더 알아야 할 것이 있는지 찾아줘.
##### 학습한 것
- 이미 구현되어 있는 swap 함수에 앞서 배운 제네릭을 사용해 문제 해결
  - 여러 개의 제네릭도 네이밍은 자유롭게 가능했다. T1, T2, T, U etc..
- 제네릭 타입 추론
  - 타입스크립트는 함수에 전달된 인자의 값을 보고 제네릭 타입을 스스로 유추하는 기능이 있다
  - 설계할 때에는 복잡해도, 실제 사용할 때는 일반 자바스크립트 함수처럼 사용할 수 있다.
- 튜플
  - 일반 배열 (T | U)[] 와 [U, T] 의 차이
  - 배열은 길이에 제한이 없고, 각 자리의 타입이 보장되지 않는다.
  - 튜플은 길이가 고정이며, 첫번째는 무조건 U, 두번째는 무조건 T라고 엄격하게 강제한다.
- 라벨링된 튜플
  - type Coordinates = [x: number, y: number]; 와 같은 식으로 튜플의 각 위치별로 라벨을 붙일 수 있다.
- 읽기 전용 튜플
  - readonly 키워드를 이용해 실수로 바꾸는 것을 방지할 수 있다.
  - function getVersion(): readonly [number, number, number] { return [1, 2, 3]; } 이후에 version[0] = 2; 를 넣으면 에러

#### 8번 문제
##### AI gems 질문과 답변 키워드
1. 좋아. 공식문서의 교차타입 부분을 읽고 금방 해결했어. 내 코드에서 내가 더 알아야할 키워드가 있을까?
- 식별 가능한 유니언과 switch문의 환상적인 짝꿍
  - logPerson 함수가 3개의 독립적인 if문과 사용자 정의 타입 가드를 사용하고 있다. 이름표 type이 달려있는 유니언 타입을 내로잉 할 떄는 switch 문을 사용할 수 있다. 이 경우, never 타입을 활용한 망라성 검사를 default 케이스에 적용하기도 편해진다.
##### 학습한 것
- 교차 타입 (intersection types)
  - 유니언 타입( | )이 A or B 의 느낌이었다면, 교차 타입은 & 기호를 사용해서 A and B로 두 개 이상의 객체 타입을 하나로 융합하는 기호이다. 이를 활용해 User와 Admin에서 type을 제거해서 합친 뒤, type은 powerUser로 추가로 지정해주어 두 속성을 합치고, type 라벨링을 따로 했다. 처음에는 Omit<User & Admin, "type">과 같이 선언했는데, 이는 오류가 발생했다. 타입스크립트가 type 속성을 동시에 user면서 admin인 값으로 만드려고 시도하는데, 이는 불가능해 User & Admini 전체를 never 타입으로 찌그러뜨려 버림. 객체가 붕괴된 이후에 Omit으로 type이 제거되므로 순서가 늦은 것.

#### 9번 문제
##### AI gems 질문과 답변 키워드
1. 좋아. 앞서 배운 제네릭을 활용해 ApiResponse로 User와 Admin의 Response를 하나로 합치는 데 성공했어. 여기서 내가 더 알아야 할 것이 있을까?
- 제네릭 기본값
  - 함수의 매개변수에 기본값을 줄 수 있듯이, 제네릭에도 기본값을 줄 수 있다.
  - ex) <T = any>와 같이 = 기호를 사용해 기본 타입을 지정
  - 만약 API가 10개 중 9개가 User[]를 반환한다면, 매번 ApiResponse<User[]>와 같이 적기 귀찮으므로, <T = User[]>와 같이 정의해두면, ApiResponse라고만 적어도 알아서 User[]로 인식하게 만들 수 있다
- 콜백 지옥
  - 자바스크립트에서 비동기 작업을 처리할 때, 콜백 함수를 계속 넘겨주는 방식을 쓰면 코드가 무한히 깊어지는 콜백 지옥에 빠지게 된다. 이를 해결하기 위해 Promise와 async / await 문법을 사용한다. 콜백함수 대신 Promise 객체를 반환하게 만들면, 타입은 Promise<ApiResponse<User[]>>와 같이 제네릭이 두 번 감싸진 형태가 된다.
##### 학습한 것
- 제네릭을 활용한 Type Wrapper
  - 제네릭의 T를 함수 내부에서 그대로 받아서 사용할 수 있음을 알게되었다.
  - coffemachine과 관련해서, 무조건 status error만 뱉고 있는데, 왜 타입인 number를 굳이 적어주어야 하는가?
    - 선언할 때 ApiResponse<T>와 같이 적었을 경우, 선언부에서 반드시 제네릭을 지정해주어야 함을 알았다. 함수의 필수 매개변수와 같은 느낌, ex) 밥먹기(메뉴) 와 같이 만들어진 함수를 밥먹기()로 호출하면 에러가 뜨는 사례
    - 콜백의 타입은 이 함수가 끝나면 커피 대기열 숫자를 담은 성공 응답이나, 실패 응답 중 하나를 줄 것이라고 외부 세계와 굳게 약속하는 부분이고, 현재 임시 구현체인 함수 내부에는 서버가 없어서 임시로 에러가 나는 상황만 mock하고 있을 뿐이므로, 이 함수가 무엇을 반환하기로 약속된 함수인지 명시해주어야 한다.

### 2. 전략 평가
- 효과적이었던 것과 그 이유
  - AI에게 지나친 힌트를 주지 않도록 강제해서 조금 더 공식문서를 정독하고 익힐 수 있는 시간이 늘어난 것 같다.
  - 문제를 풀고 난 뒤 추가적인 키워드를 요청해 추가학습 하는 것이 개념확장과 의문해결에 정말 도움이 많이 되었다.
- 비효과적이었던 것과 그 이유
  - 현재 방법이 꽤나 잘맞아서 괜찮은 것 같다.

### 3. AI 피드백
- 자신의 학습 전략에 대해 AI 학습 전문가에게 피드백을 요청하고, 유용했던 제안 1가지 이상 기록

"메타인지를 통한 자기주도적 통제력 확보와 개념의 점진적 확장, 본질에 대한 탐구심 가지기"

### 4. 다음 타임에 바꿀 것
- 유지할 것과 그 이유
  - 문제 풀이 이후 AI에게 추가 키워드 학습 요청하기 : 가지뻗기식 학습으로 너무 도움이 많이 되고 있다.
- 바꿀 것과 그 이유
  - 고무오리 디버깅 기법에 AI 활용하기 : AI 추천으로 새로운 키워드 학습 이후 "내가 이해한 방식이 맞는지 검증해 줘"라고  요청하기, 이를 통해 더 확실한 이해로 넘어갈 수 있어보인다.
---

## 학습 로그 #4

**시간**: 03/26 11:15 ~ 16:20 (점심시간, 테코톡 제외 약 220분)
**학습 범위**: typescript-exercises #10~#12

### 1. 이번 타임의 학습 전략
- 이전에 바꾸기로 한 전략은 무엇이었고, 실행했는가?
  - 추가 키워드 학습 이후, 내가 이해한 바가 맞는지 한번 더 검증하기
- 실제로 어떻게 학습했는지 디테일한 과정을 써보세요. 
#### 10번 문제
##### AI gems 질문과 답변 키워드
1. 자바스크립트의 비동기 동작 원리(Promise)와 타입스크립트의 고급 타입 조작 기술에 관해 알려줘!
- 과거 콜백함수를 이용했지만, 코드가 깊어지는 콜백 지옥을 피하기 위해 등장한 것이 Promise
- Promise는 현실 세계의 진동벨과 같은 개념. <- 언젠가 커피를 주겠다 와 같은 약속을 받은 것.
- Promise의 3가지 상태
  - Pending (대기) : 진동벨을 받고 기다리는 중
  - Fulfilled (이행 - resolve) : 진동벨이 울리고 커피가 나옴
  - Rejected (거부 - reject) : 직원이 원두가 떨어졌다고 환불해 줌
2. pr 제출까지 시간이 얼마 안남아서, 일단 고난이도 문제는 추후로 미루고 11번으로 넘어가려고 하는데, 고난이도를 제외하고 내가 여기서 추가로 알면 좋을 키워드 알려줘!
- try / catch (비동기 에러 핸들링의 표준)
  - try 안에서 await를 사용해 하나라도 에러 reject가 터지면 catch 블록으로 넘어가게 설계하는 것이 표준이다.
- Promise Unwrapping과 Awaited<T>
  - await api.requestAdmins()를 호출했을 때, 타입스크립트는 똑똑하게 Promise<Admin[]>의 껍데기를 Unwrapping하고 Admin[] 타입만 반환한다.
  - 만약 Promise 안에 어떤 타입이 들어있는지 확인하고 싶다면 Awaited<T>라는 유틸리티 타입을 사용할 수 있다.
- util.promisify
  - 실무에서 너무 자주 쓰이는 패턴이라 node.js에서 기본 기능으로 제공하고 있음
  - 추후 내장 함수나 라이브러리를 쓸 때 그 내부 원리를 완벽하게 통제할 수 있는 학습임

##### 학습한 것
- new Promise((resolve, reject) => {...}); 와 같은 형태로 promisify 함수를 일단 형태를 만들어서 진행해보았다. 기본적인 문법 오류를 수정해나감
- promisify에 집어넣어야 할 것은 oldApi.requestAdmins라는 함수이며, 이 함수의 타입은 void이다. 콜백을 받고 아무것도 반환하지 않으므로. (callback: (response: ApiResponse<Admin[]>) => void)의 형태.
- api.requestAdmins() 는 ()로 함수처럼 호출되고 있다. promisify가 Promise 객체를 바로 뱉는 형식이 아니라, 실행하면 Promise를 뱉어내는 새로운 함수를 반환해야 한다. 그러므로 () => Promise<T>와 같은 형태로 함수로 반환해야 한다.
- promise를 resolve하는 곳에서 함수 arg를 받아서 실행해야 하는데, 이 함수는 인자로 콜백 함수를 원한다. arg((response) => {response.status를 검사하는 로직}

#### 11번 문제
##### AI gems 질문과 답변 키워드
1. 타입스크립트 공식문서 ambient-modules 부분을 참고해서 타입스크립트의 declarations에 타입 선언을 하는 문법을 알려줘. 
- .d.ts 파일 (declaration files)
  - 실제 코드가 동작하는 곳이 아님. 타입스크립트 컴파일러에게 "저기 있는 자바스크립트 파일은 이런 모양의 함수들을 가지고 있어!" 하고 알려주기만 하는(declare) 설명서이다.
- declare module 키워드
  - 외부 패키지(모듈)의 껍데기를 만들 때 사용하는 전용 문법
  - ex) math-magic이라는 타입 없는 JS 라이브러리가 있다면, 다음과 같이 선언함
```
// math-magic 모듈은 이런 기능들을 가지고 있다고 내가 '선언(declare)'할게!
declare module 'math-magic' {
    export function add(a: number, b: number): number;
    // 실제 구현 코드 { return a + b } 는 적지 않습니다! 오직 껍데기(타입)만 적습니다.
}
```
2. 좋아! declare module을 사용해서 .d.ts 파일을 수정함으로써 간단하게 해결했어! 내가 추가로 알아야할 키워드 알려줘
- 타입 별칭 사용
  - 타입 선언의 중복을 피하고, 타입 별칭을 사용함으로써 무식하게 함수별로 타입을 지정해주는 것이 아닌, 공통된 타입을 따로 빼와서 가독성을 높힐 수 있다.
- DefinitelyTyped (@types/... 패키지)
  - 실무에서 타입이 없는 옛날 라이브러리를 발견했을 때, 무작정 declarations 폴더부터 만들고 .d.ts파일을 직접 짜는 것으느 사실 최후의 수단이다.
  - 전 세계의 개발자들이 옛날 JS 라이브러리들 타입 껍데기 다 만들어 놨어! 하고 모아둔 거대한 오픈소스 저장소가 DefinitelyTyped이다.
  - 실무 꿀팁 : lodash, express와 같은 유명한 라이브러리를 쓸 때는, 터머널에 npm install -D @types/lodash와 같이 누군가 만들어놓은 껍데기를 다운로드받을 수 있다.
- 앰비언트 선언 (Ambient Declarations)
  - declare 키워드의 정식 명칭이다. Ambient는 주변의, 환경의 라는 뜻이다.
  - 타입스크립트 컴파일러에게 이 코드는 컴파일하지 않아도 돼! 브라우저나 노드 환경(런타임)에 무조건 존재하니까 에러를 내지 말고 믿어! 라고 보증서를 써주는 키워드이다.
- 모듈 보강 (Module Augmentation)
  - 현재 예제는 타입이 아예 없는 라이브러리에 껍데기를 씌운 것. 타입이 이미 예쁘게 씌워져있는 라이브러리에 나만의 커스텀 속성을 하나 추가하고 싶을 때에는 어떻게 하는가?
  - 백엔드 프레임워크인 Express의 Request 객체에는 원래 user라는 속성이 없지만, 로그인 기능을 만들고 나면 req.user = 데이터 처럼 쓰고 싶어진다. 이때 기존 모듈을 열어서 내 타입을 덧붙이는 기술을 모듈 보강이라고 부른다.
##### 학습한 것
- delare module을 사용해서 JS 로만 짜여진 코드에 타입스크립트 껍데기를 입히는 방법을 알게 되었다.
- 타입스크립트 껍데기가 공통되는 경우, 여기서도 타입 별칭을 통해 리팩토링할 수 있음을 알게 되었다.
  - 왜 기존에는 function으로 정리되던 타입이 타입 별칭으로 묶으면 const가 되는가?
  - 타입스크립트의 엄격한 문법 규칙 때문에 function의 경우 반드시 소괄호와 매개변수가 필요하다. 반면, const는 변수를 만드는 키워드로, 타입 별칭만 딱 붙여주는 것이 자연스럽다.

#### 12번 문제
##### AI gems 질문과 답변 키워드
1. 지금 declare module에 index.js 파일을 읽고, 형태에 맞춰서 comparator는 number로 넣었는데, input에는 User[]나 Admin[]이 들어오는 것으로 보여. 또, 반환되는 결과값은 null 혹은 input[index]와 같은 식으로 반환되는데, 이런 경우에 타입을 어떻게 선언해야 할까?
- 제네릭 배열
  - User[], Admin[] 모두가 들어올 수 있으니 제네릭 배열 T[]로 선언해야 한다.
- 고차 함수
  - comparator(비교기) 와 같은 보통 함수는 배열 안의 요소 두 개 (a, b)를 받아서 숫자 number를 반환하는 함수이다.
  - getValue와 같은 함수는 배열의 아이템 T를 하나 받아서 더할 수 있는 숫자로 반환해주는 함수이다. (item: T) => number
  - 위와 같은 고차 함수를 중복으로 사용할 때 타입 별칭을 이용해서 따로 빼두는 것이 사용에 용이하다.
2. 좋아. 12번문제까지 완료했어. 내가 여기서 더 알면 좋을 키워드가 있을까?
- 읽기 전용 배열 (readonly T[])
  - 통계함수의 본질은 원본 데이터를 건드리지 않고 계산만 하는 것이므로 매개변수 타입을 input: readonly T[] 또는 input: ReadonlyArray<T>로 선언함으로써 강력하게 방어할 수 있다. 안정성이 극대화된다.
- 네이밍 컨벤션
  - 변수나 함수 이름은 소문자로 시작하는 카멜 케이스를 사용하지만, "타입이나 인터페이스"의 이름은 무조건 대문자로 시작하는 파스칼 케이스를 사용한다. 
##### 학습한 것
- 리버스 엔지니어링
  - 기존 JS 코드를 보고 타입스크립트 껍데기를 입히는 방법에 대해 입문했다.
- 객체타입에 대한 declare module 선언법
  - comparator와 같이 두 대상을 비교해서 숫자를 뱉어내는 함수의 경우, 단순히 결과값 number만 선언하는 것이 아니라, (a: T, b: T) => number 와 같이 두 개의 제네릭 타입을 비교해서 최종적으로 숫자를 넘겨야한다고 명시적으로 나타내주어야 한다.

### 2. 전략 평가
- 효과적이었던 것과 그 이유
  - 추가학습 키워드 가지뻗기식 학습이 마지막 세트에 가장 효과적이었다. 앞서 제한을 걸지 않았을 때에는 뒤에 나올 예제들을 미리 보고 온것인지 예습식의 키워드였는데, 후반부에는 실무에서 실제 적용될 때는 어떠한 패턴이 유용하다 와 같이 키워드를 뽑아주고 설명해주어 정말 큰 도움이 되었다고 생각한다.
- 비효과적이었던 것과 그 이유
  - 예제 생성 기능은 마지막에 지식 체크를 위해 계속 나왔는데, 막상 마감에 닥치고 시간적 여유가 없으니 해볼 엄두가 나지 않았다. 시간적인 여유가 날때나 진행해볼 수 있을 것 같아 현상태에선 매우 비효과적이었다.

### 3. AI 피드백
- 자신의 학습 전략에 대해 AI 학습 전문가에게 피드백을 요청하고, 유용했던 제안 1가지 이상 기록

오픈소스 타입 뜯어보기 (DefinitelyTyped 탐험) : 리버스 엔지니어링에 입문하셨으니, 이제 깃허브에서 DefinitelyTyped 레포지토리를 방문해 보세요. 실무에서 가장 많이 쓰는 lodash나 express의 .d.ts 파일이 어떻게 생겼는지 구경하는 것만으로도 타입 설계의 시야가 기하급수적으로 넓어집니다.

### 4. 다음 타임에 바꿀 것
- 유지할 것과 그 이유
  - 키워드 기반 학습과 현재 익힌 키워드 기반 가지뻗기식 연계학습 방식이 마음에 들어 유지하고 싶다.
- 바꿀 것과 그 이유
  - 예제 생성과 학습은 시간이 꽤 오래걸리는 비교적 비효율적 학습으로 받아들여져 여유가 생기면 그때서나 진행할 수 있을 것 같다.
...
