# 학습 로그

## 메타인지 하기

### 1. 현재 TypeScript 경험

- TS를 써본 적이 있는가? 있다면 어느 정도?

### 1. 기초 (Beginner)

- [x] 기본 타입(string, number, boolean, null, undefined)의 차이를 설명할 수 있다.
- [x] interface와 type alias의 차이점과 각각의 사용 사례를 안다.
- [x] 함수의 매개변수, 반환값, 선택적 매개변수(?)에 타입을 지정할 수 있다.
- [x] 배열(number[])과 튜플([number, string])의 구조적 차이를 이해한다.
- [x] 왜 any 사용을 지양해야 하는지, unknown과의 차이가 무엇인지 안다.

### 2. 중급 (Intermediate)

- [x] 제네릭(Generics)을 사용해 재사용 가능한 함수나 클래스를 설계할 수 있다.
- [x] 유니온(|)과 인터섹션(&) 타입을 활용해 데이터를 조합할 수 있다.
- [ ] 타입 추론의 원리를 이해하고, 'as' 단언(Assertion)의 위험성을 설명할 수 있다.
- [ ] 타입 가드(typeof, instanceof, in, 사용자 정의 가드)로 타입을 좁힐 수 있다.
- [x] Utility Types(Partial, Pick, Omit, Record 등)를 적재적소에 사용한다.
- [ ] Enum의 한계를 이해하고 as const나 Union type으로 대체할 수 있다.

### 3. 고급 (Advanced)

- [ ] 조건부 타입(Conditional Types: T extends U ? X : Y)을 구현할 수 있다.
- [ ] infer 키워드를 사용하여 조건부 타입 내에서 특정 타입을 추출할 수 있다.
- [x] 매핑된 타입(Mapped Types)과 keyof를 조합해 기존 타입을 변형할 수 있다.
- [x] 템플릿 리터럴 타입을 활용해 정교한 문자열 패턴 타입을 정의할 수 있다.
- [ ] 공변성(Covariance)과 반공변성(Contravariance)의 개념을 이해한다.
- [ ] Module Augmentation으로 외부 라이브러리의 타입 정의를 확장할 수 있다.

### 4. 실전 및 환경 (Ecosystem)

- [ ] tsconfig.json의 주요 옵션(strict, paths, target 등)의 역할을 안다.
- [ ] 타입 정의가 없는 라이브러리에 대해 직접 .d.ts 파일을 작성할 수 있다.
- [ ] React 환경에서 Props, Event, useRef 등에 정확한 타입을 입힐 수 있다.
- [x] 컴포넌트 설계 시 제네릭을 활용하여 Props의 유연성을 확보할 수 있다.

### 2. 기존에 새로운 개념을 학습할 때 어떻게 했나요? (수업시간에 v1 떠올렸던 것 첨부)

> 1. 책/강의 등 떠오른 지식 공유자를 선택해서 이론 학습을 한다.
> 2. 그러다보면 따분히 앉아있는 시간을 버틸 수 없어진다...
> 3. TS, tailwind처럼 직접 적용해 볼 수 있는 것들은 직접 시도해보면서 감을 잡아간다.
> 4. 그럴 수 없거나 막히는 부분은 대체로 AI에게 물어본다.
> 5. AI에게는 주로 내가 이해하고 있는 개념이 맞는지를 물어본다.
> 6. AI가 답변을 제대로 못하면 다시 책을 찾아보거나 공식 문서를 찾아본다. 블로그보다는 공식문서를 보게 된 것 같다. 강의를 듣는 경우 강의자에게 질문하기도 한다.

## 학습 로그 #1

**시간**: 03/25 15:43 ~ 16:00 (약 17분)
**학습 범위**: typescript-exercises #6

### 1. 첫 학습 전략 설계

- 어떤 전략으로 학습하기로 했는가?

> 0. 왜 학습해야 하는지 / 그래서 어떤 상태가 될 것인지 목표를 설정한다
> 1. 먼저 풀어본다 / 생각해본다
> 2. 7~10분 막히면 기록한다 (막힌 지점, 현재 가설 등) <--- 루프 시작
> 3. 키워드를 1~3개로 줄인다
> 4. 15~30분만 학습한다
> 5. 규칙 1줄 / 예시 1쌍 / 다음 행동 1개가 나오면 멈춘다 ---> 루프 끝
> 6. 2번으로 돌아가 반복한다
> 7. 같은 실패가 2번 반복되면 범위를 바꾼다
> 8. 3루프 또는 60~90분이면 종료한다
> 9. 루프마다 가설(Hypothesis)/증거(Evidence)/다음 단계(Next)을 기록한다
> 10. 필요한 경우 AI를 사용한다

- 그 전략을 선택한 이유는?

> 이번 학습 전략의 핵심 키워드는 **목적 의식**과 **빠른 피드백 루프**입니다.
>
> 목적 의식 없는 학습은 쉽게 방황하게 된다는 걸 느꼈고, 의식적으로 왜 이렇게 되는가?라는 질문을 가지고, 그래서 어떤 상태가 될 것인가?까지 나아가는 목표 설정을 수립하는 것을 1순위로 생각했습니다.
>
> 그렇게 설정한 학습 방향이 여전히 최선의, 최적의 방향이라는 보장은 없기 때문에 엄한 길로 빠지지 않도록 빠른 피드백 루프를 구축하고자 했습니다. 이건 레벨1 동안 여러 방면에서 시도하고 있는 가치인데요, 대주제와 소주제를 설정하고, 끊임없이 소주제를 피드백하고 재설정하는 과정을 경험해볼 수 있도록 설계했습니다.
>
> 학습 전략 구축 과정에서 기존 전략이 '언제' 그리고 '왜' 다음 단계로 넘어가는지 경계가 예리하지 않다는 피드백을 받았고, 시간이나 상태를 통해 그 기준을 세우고자 노력했습니다.

- 실제로 어떻게 학습했는지 디테일한 과정을 써보세요.

> 6번 문제를 풀다 막혔을 때, 문제 범위(키워드)를 **타입 좁히기 > 조건부 타입 or 제네릭
**으로 설정했습니다. 책과 공식 문서를 통해 짧게 학습한 결과 해당 개념으로는 문제를 해결할 수 없었습니다.
>
> 키워드 설정이 문제라는 것을 느끼고 빠르게 키워드를 함수 오버로딩으로 수정했습니다. 이 과정에서 문제 하단의 주석된 힌트와 AI를 활용했습니다.
>
> 오버로딩 적용 과정에서도 문법에 대한 이해 부족으로 어려움을 겪었는데요, 문제가 생기면 바로 2번으로 돌아가 진행했기 때문에 한곳에 정체되어 있는 시간을 최소화할 수 있었던 것 같습니다.
>
> 아래는 학습하면서 기록한 내용입니다.

```markdown
## 목표

- 왜: 하나의 함수에서 명백하게 분리된 타입을 반환해야 할 때 어떻게 할 것인가? (not 유니온)
- 그래서: a -> A / b -> B를 반환하는 함수가 a | b -> A | B 유니온 타입을 반환하는 대신 명확하게 각 타입을 반환하도록 작성할 수 있다.

## Loop 1

- H: 문제 해결을 위해 타입 좁히기 -> 조건부 타입/제네릭 부분을 학습해야 할 것이다.
- E: 조건부 타입/제네릭을 적용해도 유니온 타입으로 인해 해결이 안 됨
- N: 핵심 키워드를 재설정

## Loop 2

- H: 함수 오버로딩을 학습하면 문제를 해결할 수 있을 것이다.
- E: 함수를 여러 개 작성했더니 문법 오류가 났다. (Cannot redeclare exported variable filterPersons)
- N: 문법 오류가 왜 생겼는지 파악하고 오류 수정하기

## Loop 3

- H: 함수 오버로딩은 함수를 여러 개 선언하는 것이 아니다.
- E: 구현부는 하나로 두고, 함수 시그니처를 작성해야 한다는 개념 확인
- N: 함수를 여러 개 작성하는 대신 함수 시그니처를 작성해 타입을 분기하도록 코드 수정

## 결과

하나의 함수에서 명백하게 분리된 타입을 반환해야 할 때 어떻게 할 것인가?

- 입력 값에 따라 명확하게 타입이 분기된다면 유니온 타입으로 두기보다는 더 명시적으로 타입을 좁히는 방법이 필요하다.
- 조건부 타입, 제네릭 등의 방법과 함께 함수 시그니처 오버로딩을 고려해 본다.
```

### 2. 전략 평가

- 효과적이었던 것과 그 이유
    - 좁은 키워드 설정: 막연하게 문제를 바라보는 대신 체계적으로 문제를 쪼개 볼 수 있었고, 학습 체계를 세우기 전 문제 풀이를 할 때 방황하던 것에 비해 조건부 타입/제네릭 -> 함수 오버로딩으로 방향성을 빠르게 바로잡을 수 있었습니다.
    - HEN 전략: 현재 가설(Hypothesis), 그게 맞거나 또는 틀린 증거(Evidence), 다음에 검증할 것(Next)을 매 루프마다 남김으로써 역시 학습의 방향성을 빠르게 잡고, 잘못된 방향을 수정하는 데 도움이 되었습니다.
- 비효과적이었던 것과 그 이유
    - GPTs 사용: 비효과적이었다기보다는 약간 고민되는 지점이 있습니다. GPTs를 사용하다 보면 GPTs가 제공하는 꼬리 질문들을 자연스럽게 따라가게 되고, 그 과정에서 혼자 사고하는 힘을 기르기 어려울 수 있겠다는 생각이 들었어요. 하지만 그게 빠른 방법일 것 같기는 합니다.
    - HEN 기반 루프 기록: 가설, 근거, 다음 시도만으로는 전체 루프를 잘 보여주지 못한다는 느낌이 들었습니다. 가설, 시도, 결과, 다음 가설 등과 같이 기록을 변경해 봐도 좋을 것 같습니다.

### 3. AI 피드백

- “키워드 1~3개”는 아직 넓다 -> 키워드 1개 + 핵심 질문 1개로 수정
- HEN을 **증상 → 가설 → 실험 → 판정 → 규칙**으로 확장

### 4. 다음 타임에 바꿀 것

- 유지할 것과 그 이유
    - 현재까지는 구체적인 목표와 빠른 피드백 루프를 잘 구현한 체계라고 생각되어서 전체적인 흐름은 유지할 계획입니다.
- 바꿀 것과 그 이유
    - 다만, 기록에 빈틈이 있다는 부분을 저도 AI도 인지했으므로, HEN을 **문제 상황 -> 가설 -> 실험 내용 -> 실험 결과 -> 결과 도출**로 확장해보겠습니다.
    - 키워드 역시 1-3개라고 했지만 1개로 잡고 추가 공부를 하거나, 루프를 돌리는 게 좋겠다고 판단, 반영하겠습니다.

---

## 학습 로그 #2

**시간**: 3/26 12:45 ~ 12:50 (약 5분)
**학습 범위**: typescript-exercises #8

### 1. 이번 타임의 학습 전략

- 이전에 바꾸기로 한 전략은 무엇이었고, 실행했는가?
    - 키워드를 딱 1개만 설정하고 학습에 들어가기로 변경했고, 문제를 봤을 때 '유니온 말고 다른 문법이 있었는데...'라는 생각을 거쳐 'ts에서의 합집합과 교집합'이라는 키워드를 잡았습니다. 엄밀히 말하면 두 개의 키워드라고 볼 수도 있겠지만... 항상 헷갈리는 개념이었기에 개별로 학습하는 건 의미가 없다고 판단했습니다. 하나의 키워드만 생각하기, 실행했습니다.
    - HEN을 확장하는 방안을 적용했습니다.
- 실제로 어떻게 학습했는지 디테일한 과정을 써보세요.

> 해당 문법이 존재하는 걸 아는데 정확히 무엇이었는지 기억이 안 난 경우라 가볍게 학습 전략을 적용하고 문제를 풀었습니다.
>
> 솔직히 말하면 여기에는 루프 기록을 작성할 게 없을 것 같다,고 생각했었는데, 그래도 이 과정이 '단순 문제 풀이'를 '타입스크립트에 대한 일반적인 이해'로 확장하는 과정이라는 생각이 들어서 생각을 정리할 겸 작성했습니다.
>
> 결론적으로는 한 번 개념을 짚고, 정리하고 넘어갈 수 있어 좋은 판단이었다고 생각합니다.

```markdown
## 목표

- 왜: 둘 이상의 interface에 대해 모든 속성을 가지는 새 타입을 선언하려면 어떻게 해야 하는가?
- 그래서: a 속성을 가지는 인터페이스와 b 속성을 가지는 인터페이스에 대해서 a b를 가지는 인터페이스를 생성하는 대신, 각 인터페이스를 활용한 타입을 선언할 수 있다.

## Loop 1

- 문제 상황: a를 가지는 인터페이스 A, b를 가지는 인터페이스 B가 있을 때, a b를 가지는 타입을 A와 B를 사용해 표현해야 하는데, 문법이 기억이 안 났다.
- 가설: 유니온에 반대되는 개념이 있을 것이다.
- 실험 내용: ‘ts에서의 합집합과 교집합’이라는 키워드로 구글링했다.
- 실험 결과:
    - 유니온(|)은 합집합이다. A | B는 A나 B를 만족하는 타입을 허용한다.
    - 교집합은 인터섹션(&)이다. A & B는 A와 B에 있는 모든 속성을 가지는 타입을 허용한다.
- 결론: 인터섹션을 사용하면 속성 a b를 모두 가지는 타입을 기존 인터페이스를 활용해 선언할 수 있다.

## 결과

둘 이상의 interface에 대해 모든 속성을 가지는 새 타입을 선언하려면 어떻게 해야 하는가?

- a를 가지는 인터페이스 A, b를 가지는 인터페이스 B가 있을 때, 인터섹션을 사용하면 속성 a b를 모두 가지는 타입을 기존 인터페이스를 활용해 선언할 수 있다.
```

### 2. 전략 평가

사실 매우 작은 문제 해결 과정이라 효과/비효과를 판단하는 게 큰 의미는 없을 것 같습니다. 그럼에도 몇 자 적어보자면,

- 효과적이었던 것과 그 이유
    - HEN의 확장된 형태가 보다 학습 흐름을 살펴보기에 용이한 것 같습니다. 정리하는 입장에서도 실제 학습 흐름과 같아 더 어색함 없이 작성할 수 있었습니다.
- 비효과적이었던 것과 그 이유
    - 음... 하나를 콕 집어서 설명할 수는 없지만, 이 학습을 고안한 것 자체가 '엄한 데 땅굴파지 않기'에 많이 맞춰져 있다 보니 얕은 학습에는 그만한 효과가 없나 싶은 생각이 들기는 합니다.
    - 또, 너무 '문제 풀이'에만 집중하다 보니, 사실 이 뒤로 extends와의 차이, interface와 type의 차이 등 다른 탐색이 이어지지 않는다는 점이 아쉽습니다.

### 3. AI 피드백

회상형 과제를 위한 경량 모드가 필요합니다.

지금 전략은 원래 이런 문제에 강합니다:

- 어디서 막혔는지 모호한 문제
- 틀린 가설을 빨리 버려야 하는 문제
- 개념 선택과 적용을 반복해야 하는 문제

반대로 이번 같은 문제는 이런 유형입니다:

- 문법 이름이 기억 안 나는 회상형 문제
- 개념 충돌보다 기억 복구가 핵심인 문제
- 5분 안에 끝나는 문제

이럴 때는 지금 전략 전체를 다 돌리면 오버헤드가 큽니다.
시작 전에 문제 유형을 먼저 분류하세요

2번과 3번 사이에 이걸 넣는 걸 권합니다.

**2.5. 막힘 유형을 분류한다**

- 기억 안 남
- 이해 안 됨
- 적용 안 됨
- 디버깅 안 됨

### 4. 다음 타임에 바꿀 것

- 유지할 것과 그 이유
    - 크고 갈피를 못 잡는 문제에서 여전히 효과적이므로 현재 템플릿을 유지합니다.
- 바꿀 것과 그 이유
    - 작고 간단한 문제에는 과한 감이 있습니다. AI가 제안한 것처럼 유형별로 다른 학습 전략을 선택하는 건 합리적인 선택이지만, 아직 하나의 학습법도 익숙하지 않은 상태에서 가지를 나누고 싶지는 않습니다. 다만 막혔을 때 그 유형을 판단하고, 유형에 따라 마음가짐을 달리하는 것은 충분히 가능하고, 도움이 될 것 같습니다.
    - 작고 간단한 문제에서는 더 확장될 여지가 있는데 빠르게 끝나는 감이 있습니다. 더 학습할 여지가 있는 경우, 결과 하단에 다음에 학습할 내용을 추가해봐도 좋을 것 같습니다.

---

## 학습 로그 #3

**시간**: 3/26 15:24 ~ 16:44 (약 80분)
**학습 범위**: typescript-exercises #10

### 1. 이번 타임의 학습 전략

- 이전에 바꾸기로 한 전략은 무엇이었고, 실행했는가?
    - 막힘 유형을 판단하고 넘어가기로 했습니다. 이번 문제는 js 문법적인 이해가 부족했습니다.
- 실제로 어떻게 학습했는지 디테일한 과정을 써보세요.

> 이번에는 제네릭과 같은 타입스크립트 문법이 아니라 콜백 기반을 Promise로 변환하는 과정에서 필요한 js 문법 지식에서 어려움을 겪었습니다. 단순히 ts를 학습하고, 이 문제를 풀기 위한 학습을 하지 않는다는 저만의 이번 미션 목표에 따르면 오히려 좋은 상황이었던 것 같아요.
>
> 처음에 Promise의 동작 원리를 알아야겠다고 생각했는데, 그렇게 잡으니까 Promise 관련 문서를 읽기만 하고 갈피가 안 잡히는 느낌이었습니다. 당시까지 작성한 노션 내용을 기반으로 GPTs와 이야기했을 때, 범위 설정의 문제라는 피드백을 받고 'Promise 생성 시 resolve와 reject의 동작'으로 범주를 한 번 더 좁힐 수 있었습니다.
>
> 코드를 수정한 뒤에도 리팩터링을 하고 싶은 욕구를 느꼈는데요. 리팩터링을 위해 다른 라이브러리에서는 Promise를 어떻게 사용하고 있는지 확인해야겠다고 생각했습니다. axios 라이브러리의 내부 구조를 열어보면서 동작이 많이 달라 해석에 어려움을 느꼈는데, 이런 부분을 GPTs와 이야기하면서 다시 한 번 '리팩터링 욕구의 출처'에 대해 뾰족하게 고민하고, 그 문제를 어떻게 받아들일지 결정할 수 있었습니다.

```markdown
## 목표

- 왜: 콜백 기반 응답을 Promise 기반으로 변경하려면?
- 그래서: 라이브러리 없이 직접 Promise 객체를 사용해 response를 resolve할 수 있다.

## Loop 1

- 문제 상황:
  `callback-based` 를 처음 접함 + 지금까지 axios 같은 라이브러리만 사용했지, 직접 Promise 객체를 활용한 경험이 거의 없음. → 타입 관점에서는 제네릭을 활용해야겠다는 생각이 드는데, 문법적으로 막히는 상황
- 막힘 유형: /이해 안 됨 (문법)/ -> 구조 이해 안 됨
- 가설: Promise 활용 방식을 알면 아래 같은 형태로 콜백 기반 함수를 받아 Promise를 반환하는 함수를 만들 수 있을 것이다. → resolve와 reject 함수를 어떻게 사용할지 알아야 한다.

    ```jsx
    export function promisify<T>(arg: (response: T) => void): Promise<T> {
    	return new Promise((resolve, reject) => {});
    }
    ```

- 실험 내용: resolve와 reject 함수의 사용법을 알고 각각 응답 data와 error를 인자로 넘겨 호출했다.
- 실험 결과:
    - 정상적으로 Promise 객체를 반환하는 함수를 반환하는 함수가 생성됐다.

    ```jsx
    export function promisify<T>(arg: (callback: (response: ApiResponse<T>) => void) => void): () => Promise<T> {
    	return () => new Promise((resolve, reject) => {
    		arg((response) => {
    			if (response.status === 'success') {
    				resolve(response.data);
    			} else {
    				reject(response.error);
    			}
    		});
    	});
    ```

- 결론: Promise 생성자는 resolve, reject 함수를 지니는데, 각각 상태를 fullfilled/rejected 로 변경하며, Promise에 담을 데이터와 에러 메시지를 인자로 받는다.

## Loop 2

- 문제 상황: Promise를 반환하는 함수를 반환하는 형태가 너무 복잡하게 느껴진다.
- 막힘 유형: /이해 안 됨 (문법)/ -> 설계 개선
- 가설: 함수를 더 가독성 좋게 만들 방법이 있을 것이다.
- 실험 내용: axios 등 라이브러리에서 어떻게 작성하는지 살펴본다.
- 실험 결과: axios는 Promise 체인을 구현하는 등 복잡한 다른 기능들이 있지만, 이러나 저러나 Promise를 반환하는 것은 똑같다. 지금 내 함수가 복잡해 보이는 건 api 객체에서 함수를 정의하지 않았기 때문에 평상시 쓰던 동작과 다르기 때문

    ```jsx
        let promise;
        let i = 0;
        let len;
    
        if (!synchronousRequestInterceptors) {
          const chain = [dispatchRequest.bind(this), undefined];
          chain.unshift(...requestInterceptorChain);
          chain.push(...responseInterceptorChain);
          len = chain.length;
    
          promise = Promise.resolve(config);
    
          while (i < len) {
            promise = promise.then(chain[i++], chain[i++]);
          }
    
          return promise;
        }
    ```

    ```jsx
    export const api = {
    	requestAdmins: promisify(oldApi.requestAdmins),
    	requestUsers: promisify(oldApi.requestUsers),
    	requestCurrentServerTime: promisify(oldApi.requestCurrentServerTime),
    	requestCoffeeMachineQueueLength: promisify(oldApi.requestCoffeeMachineQueueLength),
    };
    ```

- 결론: 현재 구조 상 불가피한 복잡성인 것 같다. 현재 상태를 유지한다.

## 결과

콜백 기반 응답을 Promise 기반으로 변경하려면?

- Promise 콜백 안에서 resolve하면 상태가 fullfilled로 → resolve 인자로 넘긴 데이터의 Promise를 반환하게 한다.
- Promise 콜백 안에서 reject하면 상태가 rejected로 → reject 인자로 넘긴 에러 메시지를 담은 Promise를 반환하게 한다.
- 콜백 응답의 상태에 따라 resolve하거나 reject한다.

⇒ 코드의 복잡도를 판단할 때, 낯선 문법 때문인지, 구조적인 계약 때문인지 분리하는 사고가 필요하다.
```

### 2. 전략 평가

- 효과적이었던 것과 그 이유
    - AI 활용 방법(GPTs): AI에게 질문할 때에는 **핵심 개념을 힌트, 내가 잡은 핵심 개념 피드백, 내가 잡은 방향성 피드백,구체적인 문제 풀이 힌트
      ** 같은 내용만 질문하자고 스스로 정하고 GPTs에게도 그 내용을 적용했었습니다. 다른 AI와 GPTs를 같이 사용해 봤는데, 무엇을 물어봐야 할지도 감을 못 잡을 때 다른 AI는 여전히 두리뭉실한 답변을 주는 반면, GPTs는 지금 문제가 '범위 설정'에 있다는 것을 명확하게 지적하고 새로운 범위를 제안해 주었습니다. 이 부분에서 많은 시간을 단축할 수 있었던 것 같아요.
- 비효과적이었던 것과 그 이유
    - 템플릿에 맞춰 학습하다 보니 "규칙 1줄 / 예시 1쌍 / 다음 행동 1개가 나오면 멈춘다" 같은 설명을 충분히 이행하지 못하고 있다는 생각이 들었습니다. 설계와 템플릿 간의 차이를 줄여보면 좋을 것 같습니다.
    - 루프별 좁은 가설 설정으로 학습이 크게 다른 길로 새지 않고 원래 주제로 잘 돌아오고 있다는 느낌을 받았지만, '15-30분만 학습한다'는 조건을 의식적으로 적용하지는 못하고 있는 것 같습니다.

### 3. AI 피드백

- 한 가지 문제로부터 이어지는 내용일지라도 '목표'가 다르면 로그를 분리하라 (여기서는 Promise 구현 -> 리팩터링)
- 템플릿 수정

### 4. 다음 타임에 바꿀 것

- 유지할 것과 그 이유
    - 전체 흐름은 두세 번의 반복 안에서도 문제를 해결하는 힘이 늘어났다고 체감될 만큼 괜찮다고 느껴집니다.
- 바꿀 것과 그 이유
    - 문제 인식 후 문제 유형을 판단하는 ai를 하나 붙일까 고민됩니다.
    - 템플릿을 작성하는 건 '익숙하지 않은 지금, 의식적으로 템플릿을 작성하면서 나중에는 템플릿 없이도 해당 사고를 할 수 있도록'하기 위함이었습니다. 그런데 AI가 제안한 대로 템플릿을 수정하면, 한 번의 학습에서 기록에 들어가는 비용이 너무 크다는 문제가 있습니다. 현재 템플릿 사용으로 인한 효과를 체감하고 있으므로, 템플릿을 유지하고 초기 설계를 일부(규칙 1줄 / 예시 1쌍 / 다음 행동 1개 등) 수정해 볼 계획입니다.

---

## 학습 로그 #4

**시간**: MM/DD HH:mm ~ HH:mm (약 X분)
**학습 범위**: typescript-exercises #10 bonus

### 1. 이번 타임의 학습 전략

- 이전에 바꾸기로 한 전략은 무엇이었고, 실행했는가?
    - 설계 수정:
  ```markdown
  1. 왜 학습해야 하는지 / 그래서 어떤 상태가 될 것인지 목표를 설정한다
  2. 먼저 풀어본다 / 생각해본다
  3. 학습-피드백 루프를 돈다 (루프 당 15~30분, 시간 기록)
      1. 7~10분 막히면 기록한다 (막힘 유형, 막힌 지점, 현재 가설 등)
      2. 막힘 유형: 기억 안 남 / 이해 안 됨 / 적용 안 됨 / 디버깅 안 됨
      3. 핵심 질문 1개를 설정하고, 그것을 검증할 가설을 세운다
      4. 실험 → 결과 → 판정을 반복한다
      5. 아래가 만족되면 결과 작성 후 종료:
          - 규칙 1줄 설명 가능
          - 예시 1쌍 설명 가능
          - 다음 행동 1개 정의됨
      6. 2번으로 돌아가거나, 3루프 또는 60~90분이면 종료한다
  4. 같은 실패가 2번 반복되면:
      1. 먼저 추상화 레벨을 낮춘다 (최소 코드 / 예제)
      2. 그래도 안 되면 범위를 이동한다
  5. 필요한 경우 AI를 사용한다
      1. 핵심 개념 묻기
      2. 내가 설정한 핵심 개념 피드백
      3. 내가 설정한 문제 해결 방향성 피드백
      4. 구체적인 문제 풀이 힌트
    ```

- 실제로 어떻게 학습했는지 디테일한 과정을 써보세요.

```markdown
## 목표

- 왜: 어떻게 임의의 객체 타입을 추론할까?
- 그래서: 타입을 모르는 객체에 대해 타입 안전성이 보장된 코드를 작성할 수 있다.

## Loop 1 (22:39-22:59)

- 문제 상황: 객체의 value별 제네릭 타입이 필요한데 해결방안을 모르겠다. 객체의 value로부터 타입을 가져오고, value에 있는 함수로부터 파라미터 타입을 가져와야 한다.

    ```jsx
    export function promisifyAll<T extends {}>(callbackBasedApi: T) {
    	return Object.entries(callbackBasedApi).reduce((prev, [key, fn]) => {
    		prev[key] = promisify(fn);
    		return prev;
    	}, {});
    }
    ```

- 막힘 유형: 구조, 동작
- 가설: value로부터 타입을 가져올 수 있으면, 다음 단계로 넘어갈 수 있다.
- 실험 내용: 매핑된 타입을 사용해 안전한 타입을 구현한다.
- 실험 결과:
    - 규칙: 매핑된 타입을 사용하는 게 항상 객체 타입을 보장하지는 않는다.
    - 예시: 아래에서 사용된 매핑된 타입은 그냥 T를 사용하는 것과 차이가 없고, 결론적으로 fn의 타입을 보장하지 않는다.
      (단순 typeof T와 큰 차이가 없었을 것으로 보임.)

        ```jsx
        type CallbackBasedApi<T> = {
        	[K in keyof T]: T[K]
        }
        
        export function promisifyAll<T extends {}, K = keyof T>(callbackBasedApi: CallbackBasedApi<T>) {
        	return Object.entries(callbackBasedApi).reduce((prev, [key, fn]) => {
        		prev[key] = promisify(fn);
        		return prev;
        	}, {});
        }
        ```

    - 다음 행동: 현재 코드에서 타입을 보장할 수 없는 원인을 파악하고 해결하기
- 결론: 어떤 임의의 객체의 value 타입을 얻기 위해 매핑된 타입이 최선이 아닐 수 있다.

## Loop 2 (23:09-23:35)

- 문제 상황: key, fn의 타입이 T의 타입으로 좁혀지지 않는다.

    ```jsx
    export function promisifyAll<T extends {}>(callbackBasedApi: T) {
    	return Object.entries(callbackBasedApi).reduce((prev, [key, fn]) => {
    		prev[key] = promisify(fn);
    		return prev;
    	}, {});
    }
    ```

- 막힘 유형: 구조, 동작
- 가설: key, fn의 타입을 좁히기 위해 매핑된 타입 외 다른 방법을 사용할 수 있다.
- 실험 내용: reduce 대신 for-in 구문을 사용한다.
- 실험 결과:
    - 규칙: key, fn의 타입이 T에 대한 타입으로 정의되지 않고, 앞선 루프에서 매핑된 타입이 의미가 없었던 것은 Object.entries에서 타입을 넓혀버리기 때문이다.
    - 예시: key-value 쌍을 보장할 수 있는 for-in 구문을 사용하면 함수의 타입이 `T[Extract<keyof T, string>]` 으로 추론된다.

        ```jsx
        export function promisifyAll<T extends {}>(callbackBasedApi: T) {
        	const result = {} as T;
        
        	for (const key in callbackBasedApi) {
        		result[key] = promisify(callbackBasedApi[key]);
        	}
        
        	return result;
        }
        ```

    - 다음 행동: 그러나 여전히 `T[Extract<keyof T, string>]` 타입을
      `(callback: (response: ApiResponse<unknown>) => void) => void` 타입으로 연결하지 못하는 문제가 있다. 이 문제 원인을 찾고 해결한다.
- 결론: Object.entries를 사용하면 타입을 잃는다. key-value 타입 유지가 필요하다면 for-in이 좋은 대안이 될 수 있다.

## Loop 3 (09:21-09:56)

- 문제 상황: `T[Extract<keyof T, string>]` 타입을
  `(callback: (response: ApiResponse<unknown>) => void) => void` 타입으로 연결하지 못하는 문제가 있다.

    ```jsx
    export function promisifyAll<T extends {}>(callbackBasedApi: T) {
    	const result = {} as T;
    
    	for (const key in callbackBasedApi) {
    		result[key] = promisify(callbackBasedApi[key]);
    	}
    
    	return result;
    }
    ```

- 막힘 유형: 구조, 동작
- 가설: T의 타입을 좁혀서
  `T[Extract<keyof T, string>]` 타입이 더 구체적으로 추론될 수 있도록 한다. → T의 value에 함수가 있고, 그 함수의 파라미터 타입을 추론 가능하게 해야 한다.
- 실험 내용: 파라미터 타입을 추론하는 유틸리티 타입을 만든다.
- 실험 결과:
    - 규칙: infer를 사용해 파라미터 타입을 추론할 수 있다.
    - 예시:

        ```jsx
        type Callback<T> = (callback: (response: ApiResponse<T>) => void) => void;
        type CallbackApi = Record<string, (callback: (response: ApiResponse<any>) => void) => void>;
        type ParameterType<T> = T extends Callback<infer R> ? R : never
        type Promisified<T, P = ParameterType<T[keyof T]>> = Record<string, () => Promise<P>>
        
        export function promisifyAll(callbackBasedApi: CallbackApi) {
        	const result = {} as Promisified<CallbackApi>;
        
        	for (const key in callbackBasedApi) {
        		result[key] = promisify(callbackBasedApi[key]);
        	}
        
        	return result;
        }
        ```

    - 다음 행동: Callback 및 CallbackApi 타입을 작성하는 방식이 좋은 방식일까? 기존 모든 함수에 타입을 적용, 작성해야 하는 불편한 비용 vs typeof oldApi 같이 객체에 의존하는 타입 형태… → callback 객체 타입을 만들지 않는 버전으로 코드를 다시 작성해 보고 비교한다.
- 결론: -

## 결과

어떻게 임의의 객체 타입을 추론할까?

- infer를 사용해 함수의 파라미터 타입을 추론할 수 있다.
- 객체 타입을 선언할 때 key별로 다른 value 타입을 유지하려면 mapped 타입을 사용해야 한다.
- Record는 key-value 쌍을 보장하지 못하기 때문에 key마다 value의 타입이 다른 경우 좋은 선택이 아닐 수 있다.
```

---

...

- 효과적인 학습이 왜 중요한가?
- 나의 기존 학습법 v1 떠올려보기
- 공유 및 피드백
- 효과적으로 학습하는 사람들은 어떤 모습을 하고 있나요?
- 효과적인 학습자의 특징 정리
- 학습법 v2 Refactor
- 학습법 v2 활용하여 실제로 학습해보기
- 학습 회고
- 공유
- 학습법 v3 세우기
- 회고
- 강의 피드백
