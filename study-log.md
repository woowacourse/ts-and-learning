# 학습 로그

## 메타인지 하기 - 26.03.24 (화)

### 1. 현재 TypeScript 경험

> TS를 써본 적이 있는가? 있다면 어느 정도?

- TS를 사용해서 팀 프로젝트를 진행해봤지만 기억나는 부분은 타입 정의와 interface 사용, props 타입 정의 정도이다.

### 2. 기존에 새로운 개념을 학습할 때 어떻게 했나요? (수업시간에 v1 떠올렸던 것 첨부)

1. 무언가를 처음 접했을 때 우선 구글에 검색을 한다.

2. 벨로그나 티스토리 등에 작성된 글을 읽고 이해하려고 한다.

3. 주변 인물들이 있다면 주변 인물들에게 물어본다.

4. 그래도 모르거나 이해가 되지 않으면 AI에게 물어본다.
   4-1. AI에게 물어볼 때는 사소한 것 하나하나 모두 물어본다.
   4-2. AI를 절대 믿지 않는다. AI의 답변을 정답이라고 생각하지 않고 예외와 같은 경우의 수를 계속 물어보면서 싸운다.

## 학습 로그 #1

**시간**: 03/24 16:00 ~ 17:00 (약 60분)
**학습 범위**: (예: typescript-exercises #1~#3)

### 1. 첫 학습 전략 설계

#### 어떤 전략으로 학습하기로 했는가?

1. 개념 이해: 공식 문서와 자료를 통해 “왜 존재하는지” 중심으로 개념을 파악한다.
2. 실험: 직접 코드를 작성하며 다양한 케이스를 테스트하고 예상과 결과를 비교한다.
3. 실패 분석: 틀린 이유와 오개념을 정리하여 자신의 사고 모델을 수정한다.
4. 회상: 아무것도 보지 않고 코드와 개념을 다시 작성하며 스스로 재구성한다.
5. 응용: 학습한 개념을 실제 프론트엔드 상황이나 새로운 문제에 적용한다.
6. 휴식: 휴식을 통해 뇌가 무의식적으로 학습 내용을 정리하도록 한다.

- 그 전략을 선택한 이유는?
  - v2를 실제로 적용해보고 부족한 부분(실습 부족, 확장성 부족)을 개선하여 만든 전략이기 때문이다.

### [실제로 어떻게 학습했는지 디테일한 과정을 써보세요.]

### exercise-01

#### 01-1. 개념 이해

- users가 unknown[] 타입으로 정의되어 있다. User 타입도 unknown인데, logPerson 메서드의 인자인 user가 타입 User를 받고 있다. 문제를 풀기 위해서는 User가 unknown이 아닌 다른 타입이 되어야 할 것 같다. 타입 User를 users가 정의될 수 있도록 수정하면 되지 않을까?

#### 01-2. 실험

1. 타입 User를 unknown에서
   {
   name: string;
   age: number;
   ccupation: string;
   }
   로 변경했다.
2. User를 정의했으니 users가 현재 받고 있는 타입을 unknown[]에서 User[]로 변경하자.
3. logPerson 메서드 인자인 user의 타입도 unknown에서 User로 변경하자.

#### 01-3. 실패 분석

- User의 타입을 정의하니 문제가 해결되었다! 실패하지 않았다.

#### 01-4. 회상

- type을 잘 정의하고 나면 이후 동일한 타입을 참조하는 변수들의 타입을 미리 알 수 있어 효율적인 것 같다.

#### 01-5. 응용

- 그렇다면 const users는 User[]로 타입을 정의할 필요가 있을까?
  - 타입 정의를 지우고 실행해도 문제가 발생하지 않는다.
    예상 근거 1. ts가 알아서 타입을 추론할 것이다.
    예상 근거 2. ts에서 js로 변환되어도 users 객체 배열의 데이터는 변동이 없기 떼문이다.

#### 01-6. 휴식

- 하지만 지금 나는 명시적 타입 추론을 배우는 것이 아니기 때문에, 타입을 일일히 명시해야겠다는 생각이 들었다.

### 2. 전략 평가

- 효과적이었던 것과 그 이유
  - 문제를 인식하고 풀이를 시도하는 과정
- 비효과적이었던 것과 그 이유
  - 실패한 순간이 없어서 그에 대한 피드백이 부족한 것

### 3. AI 피드백

- 자신의 학습 전략에 대해 AI에게 피드백을 요청하고, 유용했던 제안 1가지 이상 기록

### 4. 다음 타임에 바꿀 것

- 유지할 것과 그 이유

1. 문제의 정해진 흐름에서 벗어나 궁금증을 가지는 것
2. 다음 단계의 주제로 확장한 것

- 바꿀 것과 그 이유

1. 실패에 대한 관점을 바꾸는 것이 좋을 것 같다.
   이유: 자연스럽게 성공하는 것이 목적이 아니라, 의도적으로 실패하는 것을 목적으로 두자.
   -> '실패 분석' 단계를 '에러 유도 및 경계 탐색'으로 변경한다.

### 5. 추가로 알게 된 사실

- 왜 메서드의 인자는 명시적 추론이 불가능한지?
  : 외부에서 들어오는 값에 대해서는 ts가 접근을 금지시키기 때문이다. (받은 값이 문자열인지, 숫자인지, 객체인지 알 수 없기 때문에)

- user 뒤에 User를 놓는 것 말고 다른 방법이 있을까?
  : 외부에서 입력을 받는 경우니까 '타입 좁히기'를 사용하면 되겠다!

- 왜 메서드의 인자는 명시적 추론이 불가능한지?
  : 외부에서 들어오는 값에 대해서는 ts가 접근을 금지시키기 때문이다. (받은 값이 문자열인지, 숫자인지, 객체인지 알 수 없기 때문에)

- user 뒤에 User를 놓는 것 말고 다른 방법이 있을까?
  : 외부에서 입력을 받는 경우니까 '타입 좁히기'를 사용하면 되겠다!

---

## 학습 로그 #2

**시간**: MM/DD HH:MM ~ HH:MM (약 \_\_분)
**학습 범위**:

### 1. 이번 타임의 학습 전략

- 이전에 바꾸기로 한 전략은 무엇이었고, 실행했는가?
- 실제로 어떻게 학습했는지 디테일한 과정을 써보세요.

### 2. 전략 평가

- 효과적이었던 것과 그 이유
- 비효과적이었던 것과 그 이유

### 3. AI 피드백

- 자신의 학습 전략에 대해 AI에게 피드백을 요청하고, 유용했던 제안 1가지 이상 기록

### 4. 다음 타임에 바꿀 것

- 유지할 것과 그 이유
- 바꿀 것과 그 이유

---

...

- 효과적인 학습이 왜 중요한가?
- 나의 기존 학습법 v1 떠올려보기
- 공유 및 피드백
- 효과적으로 학습하는 사람들은 어떤 모습을 하고 있나요?
- 효과적인 학습자의 특징 정리
- 학습법 v2 Refactor
- 학습법 v2 활용하여 실제로 학습해보기
- 학습 회고
- 공유
- 학습법 v3 세우기
- 회고
- 강의 피드백
