# 학습 로그

## 메타인지 하기 - 26.03.24 (화)

### 1. 현재 TypeScript 경험

> TS를 써본 적이 있는가? 있다면 어느 정도?

- TS를 사용해서 팀 프로젝트를 진행해봤지만 기억나는 부분은 타입 정의와 interface 사용, props 타입 정의 정도이다.

### 2. 기존에 새로운 개념을 학습할 때 어떻게 했나요? (수업시간에 v1 떠올렸던 것 첨부)

1. 무언가를 처음 접했을 때 우선 구글에 검색을 한다.

2. 벨로그나 티스토리 등에 작성된 글을 읽고 이해하려고 한다.

3. 주변 인물들이 있다면 주변 인물들에게 물어본다.

4. 그래도 모르거나 이해가 되지 않으면 AI에게 물어본다.
   4-1. AI에게 물어볼 때는 사소한 것 하나하나 모두 물어본다.
   4-2. AI를 절대 믿지 않는다. AI의 답변을 정답이라고 생각하지 않고 예외와 같은 경우의 수를 계속 물어보면서 싸운다.

## 학습 로그 #1

**시간**: 03/24 16:00 ~ 17:00 (약 60분)
**학습 범위**: (예: typescript-exercises #1~#3)

### 1. 첫 학습 전략 설계

#### 어떤 전략으로 학습하기로 했는가?

1. 개념 이해: 공식 문서와 자료를 통해 “왜 존재하는지” 중심으로 개념을 파악한다.
2. 실험: 직접 코드를 작성하며 다양한 케이스를 테스트하고 예상과 결과를 비교한다.
3. 실패 분석: 틀린 이유와 오개념을 정리하여 자신의 사고 모델을 수정한다.
4. 회상: 아무것도 보지 않고 코드와 개념을 다시 작성하며 스스로 재구성한다.
5. 응용: 학습한 개념을 실제 프론트엔드 상황이나 새로운 문제에 적용한다.
6. 휴식: 휴식을 통해 뇌가 무의식적으로 학습 내용을 정리하도록 한다.

- 그 전략을 선택한 이유는?
  - v2를 실제로 적용해보고 부족한 부분(실습 부족, 확장성 부족)을 개선하여 만든 전략이기 때문이다.

### [실제로 어떻게 학습했는지 디테일한 과정을 써보세요.]

### exercise-01

#### 01-1. 개념 이해

- users가 unknown[] 타입으로 정의되어 있다. User 타입도 unknown인데, logPerson 메서드의 인자인 user가 타입 User를 받고 있다. 문제를 풀기 위해서는 User가 unknown이 아닌 다른 타입이 되어야 할 것 같다. 타입 User를 users가 정의될 수 있도록 수정하면 되지 않을까?

#### 01-2. 실험

1. 타입 User를 unknown에서
   {
   name: string;
   age: number;
   ccupation: string;
   }
   로 변경했다.
2. User를 정의했으니 users가 현재 받고 있는 타입을 unknown[]에서 User[]로 변경하자.
3. logPerson 메서드 인자인 user의 타입도 unknown에서 User로 변경하자.

#### 01-3. 실패 분석

- User의 타입을 정의하니 문제가 해결되었다! 실패하지 않았다.

#### 01-4. 회상

- type을 잘 정의하고 나면 이후 동일한 타입을 참조하는 변수들의 타입을 미리 알 수 있어 효율적인 것 같다.

#### 01-5. 응용

- 그렇다면 const users는 User[]로 타입을 정의할 필요가 있을까?
  - 타입 정의를 지우고 실행해도 문제가 발생하지 않는다.
    예상 근거 1. ts가 알아서 타입을 추론할 것이다.
    예상 근거 2. ts에서 js로 변환되어도 users 객체 배열의 데이터는 변동이 없기 떼문이다.

#### 01-6. 휴식

- 하지만 지금 나는 명시적 타입 추론을 배우는 것이 아니기 때문에, 타입을 일일히 명시해야겠다는 생각이 들었다.

### 2. 전략 평가

- 효과적이었던 것과 그 이유
  - 문제를 인식하고 풀이를 시도하는 과정
- 비효과적이었던 것과 그 이유
  - 실패한 순간이 없어서 그에 대한 피드백이 부족한 것

### 3. AI 피드백

- 자신의 학습 전략에 대해 AI에게 피드백을 요청하고, 유용했던 제안 1가지 이상 기록

  > 문제 풀이에 한 번에 성공했다면, 실패가 찾아오기를 기다리지 말고 스스로 실패를 만들어 내야 합니다. TypeScript 학습에서 가장 훌륭한 스승은 바로 '빨간줄(에러 메시지)'입니다.

  > 현재 상황: 정답을 맞힘 -> 실패하지 않음 -> 피드백 없음

  > 개선된 피드백 루프: 정답을 맞힘 -> 의도적으로 타입을 망가뜨림 (Boundary Testing) -> TS가 뱉어내는 에러 메시지를 분석함 -> 타입스크립트의 방어선(경계)을 완벽히 이해함

### 4. 다음 타임에 바꿀 것

- 유지할 것과 그 이유

1. 문제의 정해진 흐름에서 벗어나 궁금증을 가지는 것
2. 다음 단계의 주제로 확장한 것

- 바꿀 것과 그 이유

1. 실패에 대한 관점을 바꾸는 것이 좋을 것 같다.
   이유: 자연스럽게 성공하는 것이 목적이 아니라, 의도적으로 실패하는 것을 목적으로 두자.
   -> '실패 분석' 단계를 '에러 유도 및 경계 탐색'으로 변경한다.

### 5. 추가로 알게 된 사실

- 왜 메서드의 인자는 명시적 추론이 불가능한지?
  : 외부에서 들어오는 값에 대해서는 ts가 접근을 금지시키기 때문이다. (받은 값이 문자열인지, 숫자인지, 객체인지 알 수 없기 때문에)

- user 뒤에 User를 놓는 것 말고 다른 방법이 있을까?
  : 외부에서 입력을 받는 경우니까 '타입 좁히기'를 사용하면 되겠다!

### exercise-02

#### 02-1. 개념 이해

- 현재 const persons는 객체 배열인데, 객체의 타입이 occupation을 가진 경우도 있고, role을 가진 경우가 있다. persons의 현재 타입은 User[]이기 때문에 Admin 타입에 속하는 경우 에러를 발생시키는 것이라고 생각한다. 따라서 persons는 User뿐만 아니라 Admin 타입으로도 정의될 수 있어야 하기 때문에, 유니온 타입을 사용하면 되지 않을까? 라고 생각이 들었다.

#### 02-2. 실험

1. 타입 User에 `role: string;`을 추가하면 해결되지 않을까? -> 모든 줄에 빨간줄이..
2. persons: User[]를 persons: Admin[]으로 변경하면? -> 앞에서의 똑같은 오류 발생
3. 타입 좁히기를 사용해보면? 근데 객체 선언에서부터 에러가 발생하면 타입 좁히기는 못하지 않을까? 라는 생각도 들었다.
4. persons 뒤 타입을 Person[]으로 변경해보았다. -> 객체 선언에서의 오류는 사라졌지만 맨 밑의 logPerson 메서드를 호출하는 데 오류가 발생한다.
5. 그럼 4번에서의 경우를 가만히 두고, Person 타입을 User와 Admin을 동시에 만족시켜야 하는 것 아닐까? 일단 유니온으로 테스트해보자.
6. 여전히 logPerson 메서드를 호출하면 오류가 발생한다. 인자인 user의 타입을 Person으로 바꿔보자.

#### 02-3. 실패 분석

- Person 타입을 변경하고, logPerson 메서드의 인자의 타입을 변경하니 문제가 해결되었다.
- User에 role을 추가한다면 왜 오류가 발생할까?
  <- 그렇게 되면 모든 User타입의 변수들은 role을 필수로 가져야 한다.
- 타입 좁히기는?
  <- 객체 안에서 타입 좁히기가 되나..?
- persons 뒤 타입을 Person[]으로 변경해도 왜 오류가 발생할까?
  <- unknown으로 타입을 정의한 것이나 마찬가지이기 때문이다.

#### 02-4. 회상

- 여러 타입이 정의되야 할 때는 유니온을 사용한다.
- 타입들을 위한 타입을 만든다고 생각하면 편하다.

#### 02-5. 응용

- 그렇다면 e-01과 마찬가지로 const persons: Person[]로 타입을 정의할 필요가 있을까?
  - 마찬가지로 타입 정의를 지우고 실행해도 문제가 발생하지 않는다.
    이 경우도 마찬가지로 명시적으로 추론하지 않았을까? -> 실제로 타입을 지우고 마우스 커서를 가져다대면 유니온 타입으로 추론이 된 것을 확인할 수 있었다.

#### 02-6. 휴식

- 여러 타입을 명시해야 될 때는 유니온 타입이 최선인가?

### 2. 전략 평가

- 효과적이었던 것과 그 이유
  - 가설 기반의 체계적인 에러 탐색 (예상과 결과 예측 실험)
  - IDE 기능을 활용한 타입 추론 검증 (마우스를 가져다대서 타입을 확인)
- 비효과적이었던 것과 그 이유
  - 일부로 틀리는 이런 과정이 맞는 걸까?

### 3. AI 피드백

- 자신의 학습 전략에 대해 AI에게 피드백을 요청하고, 유용했던 제안 1가지 이상 기록

  > 직관(Guess)으로 내린 결론을 '팩트 체크(Fact-check)'하는 과정이 누락되어 있습니다.

  > 02번 로그의 전반적인 흐름을 보면, 매우 훌륭한 가설을 세우고 실험했지만, 결론을 내릴 때 자신의 '짐작'으로 마무리하는 경향이 있습니다. 프론트엔드 개발에서 직관은 중요하지만, 그 직관이 공식적인 스펙과 일치하는지 검증하지 않으면 나중에 더 큰 오개념(Technical Debt)으로 돌아옵니다.

### 4. 다음 타임에 바꿀 것

- 유지할 것과 그 이유

  1. 가정하고 결과를 예측하는 실험 행동
  2. 추가적인 궁금증을 생각날때마다 정리한 것

- 바꿀 것과 그 이유

  1. 팩트체크를 진행하는 과정을 추가해야 할 것 같다.

  - 학습법 3단계 내용을 수정하자.

    `틀린 이유를 스스로 가설을 세운 뒤, 반드시 TS 공식 문서 검색, 구글링, 혹은 AI에게 에러 메시지를 복붙하여 내 가설이 맞는지 '팩트 체크'한다. 공식 용어로 내 사고 모델을 교정한다.`

### 5. 추가로 알게 된 사실

- 그렇다면 logPerson 함수 내부에서 user.role을 출력하려고 하면 에러가 날까?
  - 이를 해결하기 위해서 어떤 기술이 필요할까? : 타입 좁히기

### exercise-03

#### 03-1. 개념 이해

- 이번에는 위 타입 정의는 잘 되어 있는 것 같은데, logPerson 메서드 내부에서 오류가 엄청나게 발생한다.
  ```
  'Person' 형식에 'role' 속성이 없습니다.
  'User' 형식에 'role' 속성이 없습니다.
  ```
  라고 한다. `person.role` 부분에 마우스를 가져다대니 타입이 any라고 추론된다. 위 타입 정의에서는 분명 Person을 User와 Admin의 유니온 타입으로 정의했기 때문에, role이 있어야 하지 않나? 이유를 잘 모르겠다.

#### 03-2. 실험

- 타입 좁히기 방식이 잘못되었다고 가정하면, 일단 타입 좁히기 개념과 예제를 찾아보기
- 찾아보니 타입 좁히기에 사용하는 문법은 그냥 프로퍼티가 있는지를 if문으로 검사하는 것이 아니라, `instanceof`라는 연산자를 사용한다고 한다.
- 프로퍼티가 존재하는지 확인하는 `in` 연산자 사용하기

#### 03-3. 실패 분석

- `person instanceof Admim`이라고 작성했는데 다음과 같은 에러가 발생했다.
  > 'Admin'은(는) 형식만 참조하지만, 여기서는 값으로 사용되고 있습니다.
- 타입 인스턴스를 확인하는 게 아니라 특정 프로퍼티가 존재하는지 확인하는 방법이 필요한 것 같다. -> `in`

#### 03-4. 회상

- 프로퍼티가 존재하는지 확인하는 방법은 `in` 연산자이다.

#### 03-5. 응용

- 만약 두 타입에 모두 존재하는 name 프로퍼티를 in으로 검사한다면? 통과되지 못한 person의 타입은 never이다. (둘 다 name을 가지고 있기 때문에 else에서의 person은 도달할 수 없다.)

#### 03-6. 휴식

- in을 사용하려면 타입 간 공통되지 않는 프로퍼티가 있어야 한다.

### 2. 전략 평가

- 효과적이었던 것과 그 이유
  - 개념 이해 및 실험 단계는 잘 지켜졌다. (단계별로 흐름대로 잘 이어졌기 때문)
- 비효과적이었던 것과 그 이유
  - `회상` 단계와 `응용` 단계에서의 목적에 대한 실행이 부족한 것 같다. (다른 예제로의 응용이 쉽게 떠오르지 않음)

### 3. AI 피드백

> 🛠️ 다음 사이클을 위한 학습법 튜닝 가이드

> 현재 겪고 있는 병목(응용의 어려움)을 해결하기 위해, 자신만의 학습 도구(study-tool-log)의 4. 회상 단계를 이렇게 튜닝해 보세요.

> [수정 전] 4. 회상: 아무것도 보지 않고 코드와 개념을 다시 작성하며 재구성한다. (예: 프로퍼티 확인은 in이다.)

> [수정 후] 4. 회상 (Rubber Ducking): "왜 다른 방식은 안 되고 이 방식만 되는지"를 런타임/컴파일 타임 등의 구조적 관점에서 가상의 동료에게 말로 설명하듯 적어본다.

### 4. 다음 타임에 바꿀 것

- 유지할 것과 그 이유

  1. 에러 메시지 원문 캡처와 가설 전환

  - instanceof를 시도했다가 막혔을 때, 감으로 때려 맞히지 않고 에러 메세지를 포착하여 빠르게 피드백을 했기 때문

  2. 엣지 케이스 탐색

  - 공통 프로퍼티인 name으로 좁히기를 시도하면 남은 else 블록의 타입이 어떻게 될지 실험

- 바꿀 것과 그 이유

  1. 회상 단계

  - 단순히 코드를 다시 작성해보는 게 아니라 구조적 관점에서 설명하면서 작성해본다.

### 5. 추가로 알게 된 사실

- in 좁히기는 공통 프로퍼티가 아니라 구분 가능한 프로퍼티에서 의미가 있다.
- instanceof 사용 시 실패 이유는 ts의 컴파일 과정에서의 이유 때문이다.

---

## 학습 로그 #2

**시간**: MM/DD HH:MM ~ HH:MM (약 \_\_분)
**학습 범위**:

### 1. 이번 타임의 학습 전략

- 이전에 바꾸기로 한 전략은 무엇이었고, 실행했는가?
- 실제로 어떻게 학습했는지 디테일한 과정을 써보세요.

### 2. 전략 평가

- 효과적이었던 것과 그 이유
- 비효과적이었던 것과 그 이유

### 3. AI 피드백

- 자신의 학습 전략에 대해 AI에게 피드백을 요청하고, 유용했던 제안 1가지 이상 기록

### 4. 다음 타임에 바꿀 것

- 유지할 것과 그 이유
- 바꿀 것과 그 이유

---

...

- 효과적인 학습이 왜 중요한가?
- 나의 기존 학습법 v1 떠올려보기
- 공유 및 피드백
- 효과적으로 학습하는 사람들은 어떤 모습을 하고 있나요?
- 효과적인 학습자의 특징 정리
- 학습법 v2 Refactor
- 학습법 v2 활용하여 실제로 학습해보기
- 학습 회고
- 공유
- 학습법 v3 세우기
- 회고
- 강의 피드백
