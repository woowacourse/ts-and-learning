# 학습 로그

## 메타인지 하기

### 1. 현재 TypeScript 경험

- TS를 써본 적이 있는가? 있다면 어느 정도?
  : 있다. 프로젝트할 때 ts를 메인으로 사용했다. type, interface, generic 정도 사용한 수준이다.

### 2. 기존에 새로운 개념을 학습할 때 어떻게 했나요? (수업시간에 v1 떠올렸던 것 첨부)

- 책을 읽고, AI와 대화하며 개념의 빈 부분을 채워나간다.
- 공식 문서를 우선적으로 참고하고, AI에 컨텍스트로 제공하여 질문하며 학습한다.
- AI에 Context7 MCP를 연결해서 질문한다.
- 최신 트렌드는 유튜브/트위터에서 영상이나 글로 먼저 인지한 뒤, 직접 시도해보며 익힌다.

## 학습 로그 #1

**시간**: 03/25 11:00 ~ 11:30 (약 30분)
**학습 범위**: exercise-01 ~ exercise-03

### 1. 첫 학습 전략 설계

- 어떤 전략으로 학습하기로 했는가? 일단 문제를 풀어보면서 전략을 세워보려고 했다.
- 그 전략을 선택한 이유는? 아직 어떤 문제인지, 어떤 유형의 문제인지 몰랐기 때문이다.
- 실제 학습 과정:
  - 1, 2번은 기존에 알고 있던 지식(type, interface)으로 풀었다.
  - 3번은 처음에 `if (person.role)`로 접근했다. truthy 체크로 narrowing이 될 거라 생각했는데 타입 에러가 났다. optional chaining도 시도해봤지만 여전히 에러가 발생했다.
  - 아래 힌트를 보고 `in` operator를 통한 타입 narrowing 문제라는 것을 알게 되었다.
  - 공식문서를 보고 `in` operator 문법을 익힌 뒤 적용하여 풀었다.

### 2. 전략 평가

- 효과적이었던 것: 혼자 먼저 시도하고 → 막히면 힌트를 확인하고 → 공식문서로 문법을 익히는 순서가 자연스럽게 형성되었다.
- 비효과적이었던 것: 3번에서 `if (person.role)` 같은 여러 시도를 했는데, TypeScript의 narrowing 메커니즘을 모르는 상태에서 감으로 접근해서 시간이 걸렸다.

### 3. AI 피드백

- "혼자 시도 → 힌트 → 공식문서" 순서는 좋은 접근이다. 다만 막힐 때 너무 오래 감으로 시도하기보다, 에러 메시지를 먼저 읽고 어떤 개념이 부족한지 파악한 뒤 빠르게 힌트/문서로 넘어가는 것이 효율적이다.

### 4. 다음 타임에 바꿀 것

- 유지할 것: 혼자 먼저 시도 → 힌트 → 공식문서 순서
- 바꿀 것: 막힐 때 힌트를 더 적극적으로 활용하자

---

## 학습 로그 #2

**시간**: 03/25 13:00 ~ 14:00 (약 60분)
**학습 범위**: exercise-04 ~ exercise-05

### 1. 이번 타임의 학습 전략

- 이전에 바꾸기로 한 전략: "막히면 힌트를 적극적으로 활용하자"
- 실제 학습 과정:
  - 4번: 함수 리턴 타입을 정하는 문제 같았는데, 단순히 `Admin`, `User`를 적어도 안 되어서 힌트를 확인했다. type predicates(`person is Admin`)라는 개념을 처음 접했다. 공식문서에서 빠르게 문법을 익히고 적용하여 통과했다.
  - 5번: `Partial`을 사용하는 문제였다. 예전에 한번 써본 적이 있어서 비교적 쉽게 적용할 수 있었다.

### 2. 전략 평가

- 효과적이었던 것: 힌트를 빠르게 확인하고 공식문서로 넘어가는 전략 덕분에 새 개념(type predicates)을 빠르게 습득할 수 있었다.
- 비효과적이었던 것: 공식문서를 보고 빠르게 감만 잡고 문제를 통과했는데, 답안지를 베끼는 느낌이 들었다. 개념을 정리하는 시간이 부족했다.

### 3. AI 피드백

- 새 개념을 익힌 직후 바로 다음 문제로 넘어가기보다, 짧게라도 "이 개념이 왜 필요한지, 어떤 상황에서 쓰는지"를 한 문장으로 정리하고 넘어가면 장기 기억에 도움이 된다. 예: "type predicates는 함수 반환값으로 타입을 좁히고 싶을 때 쓴다."

### 4. 다음 타임에 바꿀 것

- 유지할 것: 막히면 힌트를 빠르게 확인하는 것
- 바꿀 것: 새 개념을 익히면 정리하면서 공부하기

---

## 학습 로그 #3

**시간**: 03/26 11:00 ~ 13:00 (약 120분)
**학습 범위**: exercise-06 ~ exercise-07

### 1. 이번 타임의 학습 전략

- 이전에 바꾸기로 한 전략: "새 개념을 익히면 정리하면서 공부하기"
- 실제 학습 과정:
  - 6번: function overloads 문제. 처음에는 이전에 배운 `Partial`과 `Omit`만으로 시도했는데, 타입체크(`tsc --noEmit`)는 통과했지만 테스트(`test.ts`)를 돌려보니 오류가 발생했다. 공식문서를 봐도 이해가 잘 안 가서, 기존에 알던 "오버로딩" 개념과 관련이 있는지 AI에게 질문하며 접근했다. 공식문서도 번역해서 읽어보고 감을 잡은 뒤 함수 오버로드를 적용하여 통과했다.
  - 7번: 제네릭 문제라 비교적 쉽게 풀었다. 제네릭 문법이 좀 헷갈려서(여러 타입 파라미터 등) AI를 활용해서 확인했다.

### 2. 전략 평가

- 효과적이었던 것: 타입체크뿐 아니라 테스트도 돌려봐야 한다는 것을 알게 되었다. 타입 레벨의 테스트가 필요하다는 인식을 갖게 된 것이 큰 수확이었다.
- 비효과적이었던 것: 6번에서 처음에 타입체크만 통과하고 끝난 줄 알았던 것. 테스트를 먼저 돌려봤으면 더 빨리 방향을 잡았을 것이다.

### 3. AI 피드백

- 새 개념을 접할 때 기존에 아는 개념과 연결짓는 접근(오버로딩 → 오버로드)은 매우 효과적이다. 앞으로도 "이건 내가 아는 XX와 비슷한가?"라고 먼저 질문하면 이해 속도가 빨라진다. 또한 `npx tsc --noEmit | grep` 으로 특정 파일만 필터링하는 습관은 좋지만, test.ts까지 함께 체크하는 것을 기본 루틴으로 만드는 것을 추천한다.

### 4. 다음 타임에 바꿀 것

- 유지할 것: AI와 대화하며 기존 지식과 연결짓기, 공식문서 번역해서 읽기
- 바꿀 것: `npx tsc --noEmit | grep`으로 타입체크와 테스트를 함께 확인하기

---

## 학습 로그 #4

**시간**: 03/26 13:00 ~ 17:00 (약 240분)
**학습 범위**: exercise-08 ~ exercise-09

### 1. 이번 타임의 학습 전략

- 이전에 바꾸기로 한 전략: "타입체크와 테스트를 함께 확인하기"
- 실제 학습 과정:
  - 8번: intersection type(`&`) 문제. 이름만 들으면 "교집합"인데, 속성을 보면 합집합처럼 보여서 혼란스러웠다. 이미 합집합은 union(`|`)으로 다뤘기 때문에 "뭐가 다른 거지?"라는 의문이 들었다. 공식문서와 AI에게 질문하면서 "값의 집합 관점에서는 교집합이지만, 요구하는 속성은 늘어난다"는 개념을 정립했다.
  - 9번: 이전에 다뤘던 개념들(generic, `ApiResponse` 타입)을 조합하여 혼자 힘으로 풀 수 있었다.

### 2. 전략 평가

- 효과적이었던 것: AI와 대화하면서 교집합/합집합 개념을 여러 각도로 질문한 것이 이해에 도움이 되었다. 9번을 혼자 풀 수 있었던 것은 이전 문제들에서 쌓인 개념이 누적된 결과라고 생각한다.
- 비효과적이었던 것: 글로만 이해하려고 하니 intersection의 개념이 계속 헷갈렸다. 예시와 도식화를 통해 시각적으로 이해하는 것이 필요했다.

### 3. AI 피드백

- 추상적인 타입 개념을 이해할 때 "값의 집합" 관점에서 벤 다이어그램을 직접 그려보는 것이 효과적이다. 예를 들어 `A & B`는 "A에도 넣을 수 있고 B에도 넣을 수 있는 값"으로 생각하면, 조건이 더 많아지니 가능한 값이 줄어든다(교집합)는 것이 직관적으로 와닿는다. 새 개념이 헷갈릴 때는 글로만 정리하지 말고 간단한 코드 예시와 다이어그램을 함께 만들어보는 것을 추천한다.

### 4. 다음 타임에 바꿀 것

- 유지할 것: AI와 대화하며 여러 각도로 질문하기, 이전 개념을 조합하여 혼자 풀어보기
- 바꿀 것: 추상적인 개념은 예시와 도식화를 통해 시각적으로 정리하기

---

- 효과적인 학습이 왜 중요한가?
- 나의 기존 학습법 v1 떠올려보기
- 공유 및 피드백
- 효과적으로 학습하는 사람들은 어떤 모습을 하고 있나요?
- 효과적인 학습자의 특징 정리
- 학습법 v2 Refactor
- 학습법 v2 활용하여 실제로 학습해보기
- 학습 회고
- 공유
- 학습법 v3 세우기
- 회고
- 강의 피드백
