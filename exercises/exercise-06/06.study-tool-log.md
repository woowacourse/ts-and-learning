문제 번호: [ #06 ]

1. 이 문제가 학습하길 의도하는 개념(내 추측)
[ return type, 부분 property의 값 종류 ]
- 현상적으로보면 수정해야 하는 부분은
  - 매개변수 타입 - personType, criteria 의 타입을 정의하고
  - 리턴 타입을 정의해야 함
- 이미 받은 매개변수인 parameter 인 persons 의 타입을 베이스로
  이 타입을 가지고 필요한 매개변수, 리턴의 타입을 좁혀서 사용하려는게 아닐까?
- 구별되는 유니온의 그 특정 키의 값을 가지고 어느 타입인지 추론하는게 필요하다

- 막히는 부분
  Also filterPersons should accept
  partial User/Admin type according to the personType.

  또한 filterPersons는 personType에 따라
  User 또는 Admin의 일부 속성(Partial)을 받아야 한다.


2. 풀이 접근
[ 객체의 부분값으로 타입 추론, Patial ]
- Person['type'] extends 'admin' ? Admin : User
  personType extends 'admin' ? Admin : User
  이렇게 personType을 가지고 받은 criteria 혹은 person의 타입을 추론하려고 했는데 실패함

3. 실제 문제 의도 (ai)
[  ]


4. 틀린 이유 (정리)
[  ]
- filterPersons의 return 타입을 틀림
  - (User[] | Admin[]) 라고 생각했는데 틀림
  - (User | Admin)[] 로 하니 타입 에러가 사라짐



5. 다음에 어떻게 할지
[  ]

---

태그
[  ]